name: Publish to PyPI

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      release-tag:
        description: Stable GitHub release tag to publish to PyPI (for example, v0.7.7)
        required: true
        type: string

permissions:
  contents: read
  actions: write
  id-token: write

jobs:
  publish:
    if: ${{ github.event_name == 'workflow_dispatch' || !github.event.release.prerelease }}
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/project/sam-doctor/
    permissions:
      contents: read
      actions: write
      id-token: write
    steps:
      - uses: actions/checkout@v7
        with:
          # A manual recovery is dispatched from main, but must build the
          # release tag supplied by the operator rather than current main.
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.release-tag || github.event.release.tag_name }}
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - run: python -m pip install build
      - run: python -m build
      - name: Publish distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          # A re-dispatch of an already-published tag is a no-op, not a
          # failure: PyPI rejects identical filenames, and re-running the
          # recovery path must stay safe.
          skip-existing: true
      - name: Trigger strict post-release launch health check
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.release-tag || github.event.release.tag_name }}
        run: |
          gh workflow run distribution-check.yml \
            --ref main \
            -f release-tag="$RELEASE_TAG" \
            -f strict-distribution-during-release=true \
            -f strict-ethical=false \
            -f min-feedback-ratio=100
