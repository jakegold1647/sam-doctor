/* Generated by scripts/build-site-rule-catalog.py from
 * src/sam_doctor/diagnostics.py and src/sam_doctor/redaction.py.
 * Do not edit by hand: regenerate instead, or the in-page demo will
 * disagree with the CLI it is demonstrating. */
window.SAM_DOCTOR_CATALOG = {
  "sam_doctor_version": "0.13.0",
  "max_evidence_length": 360,
  "max_evidence_lines": 3,
  "benign_secret_values": [
    "write",
    "read",
    "none",
    "true",
    "false",
    "admin",
    "null"
  ],
  "ansi_escape": {
    "source": "\\x1b\\[[0-9;:?]*[ -/]*[@-~]|\\x1b\\][^\\x07\\x1b]*(?:\\x07|\\x1b\\\\)|\\x1b[@-Z\\\\-_]",
    "flags": "g"
  },
  "redaction": [
    {
      "source": "\\bhttps://(?:hooks\\.slack\\.com/(?:services|workflows)/[A-Za-z0-9/_-]{8,}|discord(?:app)?\\.com/api/webhooks/[0-9]{5,}/[A-Za-z0-9._-]{8,}|[a-z0-9-]+\\.webhook\\.office\\.com/webhookb2/[A-Za-z0-9@/._-]{8,})",
      "flags": "gi",
      "name": "webhook_url",
      "replacement": "[REDACTED_WEBHOOK_URL]"
    },
    {
      "source": "(\\blogin\\b[^\\n]{0,120}?)(--password(?!-stdin)|(?<=\\s)-p)(\\s+|=)(?!-)\\S{6,}",
      "flags": "gi",
      "name": "login_password_flag",
      "replacement": "$1$2$3[REDACTED_PASSWORD]"
    },
    {
      "source": "\\b(login\\s+\\S+\\s+password)\\s+\\S+",
      "flags": "gi",
      "name": "netrc_password",
      "replacement": "$1 [REDACTED_PASSWORD]"
    },
    {
      "source": "(?<=\\s)(-u|--user)(\\s+|=)([^\\s:]{1,120}):\\S{4,}",
      "flags": "gi",
      "name": "curl_userinfo",
      "replacement": "$1$2$3:[REDACTED_PASSWORD]"
    },
    {
      "source": "\\bdckr_pat_[A-Za-z0-9_-]{16,}\\b",
      "flags": "g",
      "name": "docker_hub_token",
      "replacement": "[REDACTED_DOCKER_TOKEN]"
    },
    {
      "source": "arn:aws(?:-us-gov|-cn)?:[^\\s'\\\"`]+",
      "flags": "g",
      "name": "arn",
      "replacement": "[REDACTED_ARN]"
    },
    {
      "source": "(?<!\\d)\\d{12}(?!\\d)",
      "flags": "g",
      "name": "account_id",
      "replacement": "[REDACTED_ACCOUNT_ID]"
    },
    {
      "source": "\\b(?:AKIA|ASIA)[A-Z0-9]{16}\\b",
      "flags": "g",
      "name": "aws_access_key_id",
      "replacement": "[REDACTED_AWS_ACCESS_KEY]"
    },
    {
      "source": "\\b(?:IQoJ|FwoG)[A-Za-z0-9+/=]{50,}",
      "flags": "g",
      "name": "aws_session_token",
      "replacement": "[REDACTED_AWS_SESSION_TOKEN]"
    },
    {
      "source": "\\b(?:gh[pousr]_[A-Za-z0-9_]{20,}|github_pat_[A-Za-z0-9_]{20,})\\b",
      "flags": "g",
      "name": "github_token",
      "replacement": "[REDACTED_GITHUB_TOKEN]"
    },
    {
      "source": "\\bxox[baprs]-[A-Za-z0-9-]{10,}\\b",
      "flags": "g",
      "name": "slack_token",
      "replacement": "[REDACTED_SLACK_TOKEN]"
    },
    {
      "source": "-----BEGIN [A-Z ]*PRIVATE KEY-----(?:[\\s\\S]*?)(?:-----END [A-Z ]*PRIVATE KEY-----|$)",
      "flags": "g",
      "name": "private_key_block",
      "replacement": "[REDACTED_PRIVATE_KEY]"
    },
    {
      "source": "\\b(authorization\\s*:\\s*bearer|bearer)\\s+[A-Za-z0-9._~+/=-]{16,}",
      "flags": "gi",
      "name": "bearer_token",
      "replacement": "$1 [REDACTED_BEARER_TOKEN]"
    },
    {
      "source": "\\b(authorization\\s*:\\s*basic)\\s+[A-Za-z0-9+/=]{8,}",
      "flags": "gi",
      "name": "basic_auth",
      "replacement": "$1 [REDACTED_BASIC_AUTH]"
    },
    {
      "source": "(aws_secret_access_key|aws_session_token|github[_-]?token|access[_-]?token|api[_-]?key|password|passwd|secret|token|secret[_-]?access[_-]?key|session[_-]?token|client[_-]?secret|private[_-]?key|x-amz-signature)([\\\"'`]?\\s*[:=]\\s*)(?!\\[REDACTED)(\\\"(?:\\\\.|[^\\\"\\\\\\r\\n])*\\\"?|'(?:\\\\.|[^'\\\\\\r\\n])*'?|`(?:\\\\.|[^`\\\\\\r\\n])*`?|[^\\s'\\\"`]+)",
      "flags": "gi",
      "name": "secret_assignment",
      "replacement": "@secret-assignment"
    },
    {
      "source": "\\beyJ[A-Za-z0-9_-]{8,}\\.[A-Za-z0-9_-]{8,}\\.[A-Za-z0-9_-]{8,}\\b",
      "flags": "g",
      "name": "jwt",
      "replacement": "[REDACTED_JWT]"
    },
    {
      "source": "([\"'`])(?:[A-Z]:[\\\\/]+Users[\\\\/]+(?!runneradmin[\\\\/]AppData[\\\\/]Local[\\\\/]Temp[\\\\/])[^\\r\\n]*?|[\\\\/](?:Users|home)[\\\\/](?!runner[\\\\/]work[\\\\/])[^\\r\\n]*?)\\1",
      "flags": "gi",
      "name": "quoted_private_path",
      "replacement": "$1[REDACTED_PRIVATE_PATH]$1"
    },
    {
      "source": "(?<![A-Za-z0-9_:/])(?:[A-Z]:[\\\\/]+Users[\\\\/]+(?!runneradmin[\\\\/]AppData[\\\\/]Local[\\\\/]Temp[\\\\/])[^\\\\/\\s'\"]+|[\\\\/](?:Users|home)[\\\\/](?!runner[\\\\/]work[\\\\/])[^\\\\/\\s'\"]+)(?:[\\\\/][^\\\\/\\s'\"]+)*",
      "flags": "gi",
      "name": "private_path",
      "replacement": "[REDACTED_PRIVATE_PATH]"
    },
    {
      "source": "\\b([a-z][a-z0-9+.\\-]{1,20}://)([^\\s:/@]{1,200})(:[^\\s/@]{1,400})?@",
      "flags": "gi",
      "name": "url_credentials",
      "replacement": "@url-credentials"
    },
    {
      "source": "\\b[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}\\b",
      "flags": "g",
      "name": "email",
      "replacement": "[REDACTED_EMAIL]"
    }
  ],
  "denial_context": {
    "action": {
      "source": "not authorized to perform:?\\s*([A-Za-z0-9-]+:[A-Za-z0-9*]+)",
      "flags": ""
    },
    "principal": {
      "source": "User:\\s*\\[REDACTED_ARN\\]",
      "flags": "i"
    },
    "resource": {
      "source": "on resource:?\\s*(\\[REDACTED_ARN\\]|\\*)",
      "flags": "i"
    },
    "explicit_deny_scp": {
      "source": "explicit deny in a service control policy",
      "flags": "i"
    },
    "explicit_deny": {
      "source": "(?:with|due to) an explicit deny",
      "flags": "i"
    },
    "implicit_deny_layer": {
      "source": "because no ([a-z][a-z -]*?)\\s*polic(?:y|ies) allows",
      "flags": "i"
    }
  },
  "stabilization_context": {
    "handler_message": {
      "source": "Resource handler returned message:\\s*\"([^\"]+)\"",
      "flags": "i"
    },
    "resource_type": {
      "source": "\\b((?:AWS|Custom)::[A-Za-z0-9]+(?:::[A-Za-z0-9]+)?)\\b",
      "flags": ""
    },
    "slow_resource_hints": [
      [
        "AWS::CertificateManager::",
        "ACM certificates stay pending until their DNS or email validation completes"
      ],
      [
        "AWS::CloudFront::",
        "CloudFront distributions propagate globally and routinely take tens of minutes"
      ],
      [
        "AWS::RDS::",
        "RDS instances and clusters have long provisioning windows"
      ],
      [
        "Custom::",
        "a custom resource stabilizes only when its handler signals completion, so check the handler function's own logs"
      ]
    ]
  },
  "rules": [
    {
      "id": "github.oidc.token-request-denied",
      "title": "The GitHub Actions job cannot request an OIDC token",
      "confidence": "high",
      "patterns": [
        {
          "source": "Unable to get ID Token.*id-token:\\s*write",
          "flags": "i"
        },
        {
          "source": "id-token:\\s*write.*(?:permission|required|missing)",
          "flags": "i"
        },
        {
          "source": "Unable to get ACTIONS_ID_TOKEN_REQUEST_(?:URL|TOKEN)",
          "flags": "i"
        },
        {
          "source": "ACTIONS_ID_TOKEN_REQUEST_(?:URL|TOKEN) env variable",
          "flags": "i"
        }
      ],
      "explanation": "The workflow does not have the permission required to request a GitHub OIDC token. AWS role trust settings cannot help until the deployment job can obtain a token.",
      "verification": [
        "Add `id-token: write` to the permissions of the workflow or deployment job.",
        "Confirm no reusable-workflow or job-level permissions block overrides that setting.",
        "Rerun the job and then evaluate the AWS trust-policy conditions if STS still rejects the token."
      ],
      "documentation_url": "https://docs.github.com/actions/how-tos/secure-your-work/security-harden-deployments/oidc-in-aws",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "github.oidc.assume-role-rejected",
      "title": "GitHub Actions cannot assume the configured AWS role through OIDC",
      "confidence": "high",
      "patterns": [
        {
          "source": "Not authorized to perform:?\\s*sts:AssumeRoleWithWebIdentity",
          "flags": "i"
        },
        {
          "source": "(?:failed|error|denied|unable|not authorized).{0,80}AssumeRoleWithWebIdentity",
          "flags": "i"
        },
        {
          "source": "AssumeRoleWithWebIdentity.{0,80}(?:failed|error|denied|not authorized)",
          "flags": "i"
        }
      ],
      "explanation": "The workflow reached AWS STS but the role trust relationship did not accept the GitHub-issued OIDC token. The usual cause is a missing `id-token: write` permission, an incorrect token audience, or a `sub` condition that does not match the repository, branch, or GitHub Environment.",
      "verification": [
        "Confirm the workflow or job permissions include `id-token: write`.",
        "Check that the role trust policy accepts `token.actions.githubusercontent.com:aud` equal to `sts.amazonaws.com`.",
        "Compare the trust policy's `sub` condition with the exact branch or GitHub Environment that ran the job; newer repositories can include immutable owner and repository IDs in that claim."
      ],
      "documentation_url": "https://docs.github.com/actions/how-tos/secure-your-work/security-harden-deployments/oidc-in-aws",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "github.oidc.audience-mismatch",
      "title": "GitHub OIDC token audience does not match AWS STS",
      "confidence": "high",
      "patterns": [
        {
          "source": "Incorrect token audience",
          "flags": "i"
        },
        {
          "source": "audience.*sts\\.amazonaws\\.com",
          "flags": "i"
        }
      ],
      "explanation": "AWS rejected the token audience. GitHub Actions OIDC deployments to AWS normally require the audience to be `sts.amazonaws.com`.",
      "verification": [
        "Check the `audience` setting passed to the credentials action, if one is configured.",
        "Confirm the IAM trust policy checks the audience expected by AWS STS."
      ],
      "documentation_url": "https://github.com/aws-actions/configure-aws-credentials#oidc-configuration",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "github.oidc.provider-missing",
      "title": "The target AWS account is missing the GitHub Actions OIDC provider",
      "confidence": "high",
      "patterns": [
        {
          "source": "No OpenIDConnect provider found in your account",
          "flags": "i"
        },
        {
          "source": "OpenIDConnect provider.*token\\.actions\\.githubusercontent\\.com.*not found",
          "flags": "i"
        }
      ],
      "explanation": "AWS could not find the GitHub Actions OIDC identity provider in the account where the role is configured. A valid workflow token cannot assume the role until that provider and the role's federated principal agree.",
      "verification": [
        "Confirm the deployment is targeting the intended AWS account and role ARN.",
        "Verify that the account has an IAM OIDC provider for `token.actions.githubusercontent.com`.",
        "Check that the role trust policy names that provider ARN as its federated principal."
      ],
      "documentation_url": "https://docs.github.com/actions/how-tos/secure-your-work/security-harden-deployments/oidc-in-aws",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "lambda.ecr-image.access-denied",
      "title": "Lambda cannot access the configured ECR image",
      "confidence": "high",
      "patterns": [
        {
          "source": "Lambda does not have permission to access the ECR image",
          "flags": "i"
        },
        {
          "source": "(?:Lambda|function).{0,80}(?:permission|access).{0,80}(?:ECR|container) image",
          "flags": "i"
        }
      ],
      "explanation": "Lambda could not retrieve the container image from ECR during deployment. This usually points to a missing repository policy, a removed permission, or a cross-account configuration that no longer grants image access.",
      "verification": [
        "Confirm the function uses `PackageType: Image` and identify the exact ECR repository and image tag or digest.",
        "Check the ECR repository policy and the deployment configuration for the function's account and Region.",
        "For cross-account images, verify both the consuming account's identity policy and the owning account's repository policy allow the required image retrieval actions."
      ],
      "documentation_url": "https://docs.aws.amazon.com/lambda/latest/dg/images-create.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "codebuild.codeconnections.access-denied",
      "title": "CodeBuild cannot use the configured CodeConnections connection",
      "confidence": "high",
      "patterns": [
        {
          "source": "User is not authorized to access connection\\b.*Service:\\s*AWSCodeBuild\\b.*Error Code:\\s*OAuthProviderException\\b",
          "flags": "i"
        }
      ],
      "explanation": "CloudFormation reached AWS CodeBuild, but CodeBuild rejected the configured CodeConnections source connection while creating the project. The usual causes are a project service role missing the connection-read actions, a connection that is not available to this account or Region, or an IAM policy attachment that was not in effect before the project was created.",
      "verification": [
        "Identify the CodeBuild project service role and the connection ARN in the transformed template or project source settings.",
        "Read the service role's inline and attached policies and confirm the source mode is allowed: CodeConnections sources commonly need `codeconnections:GetConnection` and `codeconnections:GetConnectionToken`; add `codeconnections:UseConnection` when the configured service path requires it.",
        "Check the connection status and provider in the target account and Region with `aws codeconnections get-connection --connection-arn <connection-arn>` (read-only).",
        "If the role policy and CodeBuild project are created in one stack, make the project depend on the policy attachment and retry after IAM propagation; do not widen the deployment role as a substitute for the project role."
      ],
      "documentation_url": "https://docs.aws.amazon.com/codebuild/latest/userguide/connections-github-app.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "ecr.auth.login-failed",
      "title": "The CI runner could not authenticate to ECR to push the image",
      "confidence": "high",
      "patterns": [
        {
          "source": "no basic auth credentials",
          "flags": "i"
        },
        {
          "source": "Your authorization token has expired\\.?\\s*Reauthenticate",
          "flags": "i"
        },
        {
          "source": "not authorized to perform:\\s*ecr:GetAuthorizationToken",
          "flags": "i"
        }
      ],
      "explanation": "The machine running the deployment could not authenticate to ECR before or while pushing the function image. This happens earlier than any Lambda image-pull problem: the runner never logged in to the registry, its 12-hour ECR authorization token expired mid-job, or the deployment identity lacks `ecr:GetAuthorizationToken`.",
      "verification": [
        "Run `aws ecr get-login-password | docker login` (or the `aws-actions/amazon-ecr-login` action) in the job before the push step.",
        "For jobs that can run longer than 12 hours, re-authenticate before the push instead of reusing the login from the start of the job.",
        "Grant the deployment identity `ecr:GetAuthorizationToken` (its resource is always `*`) plus the repository-scoped push actions on the exact repository in the error."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AmazonECR/latest/userguide/registry_auth.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "iam.deny.explicit",
      "title": "An explicit deny blocked a deployment action",
      "confidence": "high",
      "patterns": [
        {
          "source": "(?:with|due to) an explicit deny",
          "flags": "i"
        }
      ],
      "explanation": "AWS evaluated the request and found a Deny statement that matched it. An explicit deny always wins: adding or broadening Allow policies cannot fix this. The deny lives in one of the policy layers that apply to the caller - a service control policy from the AWS Organizations management account (the error says so when it is the cause), or a Deny in an identity, resource, permissions-boundary, or session policy.",
      "verification": [
        "Record the exact action, caller, and resource from the error, and run `aws sts get-caller-identity` in the same environment to confirm which identity actually made the call.",
        "If the message names a service control policy, inspect SCPs from the AWS Organizations management account; the deny cannot be seen or changed from the member account.",
        "Otherwise search for matching `\"Effect\": \"Deny\"` statements across the caller's identity policies, permissions boundary, session policy, and the target's resource policy, and confirm the layer with the IAM Policy Simulator.",
        "Look up the request in CloudTrail (by request ID from the error when present) to see the full denied request context.",
        "Fix by amending the specific Deny (add a condition or exception for the deployment identity); do not broaden Allow policies, and never attach AdministratorAccess to work around a deny."
      ],
      "documentation_url": "https://docs.aws.amazon.com/IAM/latest/UserGuide/troubleshoot_access-denied.html",
      "suppressed_by": [],
      "excluded_line_patterns": [
        {
          "source": "AssumeRoleWithWebIdentity",
          "flags": "i"
        },
        {
          "source": "ECR image",
          "flags": "i"
        },
        {
          "source": "ecr:GetAuthorizationToken",
          "flags": "i"
        },
        {
          "source": "not authorized to perform:?\\s*[a-z0-9]+(?:-[a-z0-9]+)*:(?:(?:Tag|Untag)[a-z0-9]+|(?:Create|Delete)Tags|AddTagsToResource|RemoveTagsFromResource)(?![a-z0-9*])",
          "flags": "i"
        }
      ],
      "parse_denial_context": true,
      "parse_stabilization_context": false
    },
    {
      "id": "iam.deny.implicit",
      "title": "A deployment action was denied because no policy allows it",
      "confidence": "high",
      "patterns": [
        {
          "source": "because no [a-z -]*polic(?:y|ies) allows",
          "flags": "i"
        }
      ],
      "explanation": "AWS denied the action because no applicable policy grants it - an implicit deny. The error wording names the policy layer AWS expected the permission in (identity-based, resource-based, or session policy). The fix is a least-privilege Allow for exactly the denied action and resource in that layer, not a broader role.",
      "verification": [
        "Run `aws sts get-caller-identity` in the failing environment to confirm the request used the identity you expected (a wrong profile often looks like a missing permission).",
        "Grant the exact denied action on the exact resource in the policy layer the error names: the deploy role's identity policy, or the resource policy of the named bucket, key, or queue.",
        "Confirm the change with the IAM Policy Simulator before re-running the deployment.",
        "If it is unclear which policy applies, look up the denied event in CloudTrail (by request ID when the error includes one).",
        "Keep the grant least-privilege; never attach AdministratorAccess to make a deployment pass."
      ],
      "documentation_url": "https://docs.aws.amazon.com/IAM/latest/UserGuide/troubleshoot_access-denied.html",
      "suppressed_by": [],
      "excluded_line_patterns": [
        {
          "source": "AssumeRoleWithWebIdentity",
          "flags": "i"
        },
        {
          "source": "ECR image",
          "flags": "i"
        },
        {
          "source": "ecr:GetAuthorizationToken",
          "flags": "i"
        },
        {
          "source": "not authorized to perform:?\\s*[a-z0-9]+(?:-[a-z0-9]+)*:(?:(?:Tag|Untag)[a-z0-9]+|(?:Create|Delete)Tags|AddTagsToResource|RemoveTagsFromResource)(?![a-z0-9*])",
          "flags": "i"
        }
      ],
      "parse_denial_context": true,
      "parse_stabilization_context": false
    },
    {
      "id": "iam.access-denied.generic",
      "title": "AWS denied an API action required by the deployment",
      "confidence": "medium",
      "patterns": [
        {
          "source": "AccessDenied(?:Exception)?",
          "flags": "i"
        },
        {
          "source": "is not authorized to perform:",
          "flags": "i"
        }
      ],
      "explanation": "The active AWS identity was denied an action. The visible error may be caused by an identity policy, permissions boundary, resource policy, service control policy, or the role/session policy.",
      "verification": [
        "Record the exact action, resource, and caller ARN from the error before changing a policy.",
        "Use the IAM Policy Simulator or CloudTrail to determine which policy layer denied the request.",
        "Apply the smallest permission change that permits the intended deployment action."
      ],
      "documentation_url": "https://docs.aws.amazon.com/IAM/latest/UserGuide/troubleshoot_access-denied.html",
      "suppressed_by": [
        {
          "source": "Lambda does not have permission to access the ECR image",
          "flags": "i"
        },
        {
          "source": "(?:access has been denied by S3[\\s\\S]*permission[\\s\\S]*GetObject)",
          "flags": "i"
        },
        {
          "source": "permission to GetObject for.*bucket",
          "flags": "i"
        }
      ],
      "excluded_line_patterns": [
        {
          "source": "AssumeRoleWithWebIdentity",
          "flags": "i"
        },
        {
          "source": "ECR image",
          "flags": "i"
        },
        {
          "source": "ecr:GetAuthorizationToken",
          "flags": "i"
        },
        {
          "source": "(?:with|due to) an explicit deny",
          "flags": "i"
        },
        {
          "source": "because no [a-z -]*polic(?:y|ies) allows",
          "flags": "i"
        },
        {
          "source": "S3 error: Access ?Denied",
          "flags": "i"
        },
        {
          "source": "(?:Error|Failed) uploading to \\S+.{0,200}Access ?Denied",
          "flags": "i"
        },
        {
          "source": "not authorized to perform:?\\s*[a-z0-9]+(?:-[a-z0-9]+)*:(?:(?:Tag|Untag)[a-z0-9]+|(?:Create|Delete)Tags|AddTagsToResource|RemoveTagsFromResource)(?![a-z0-9*])",
          "flags": "i"
        },
        {
          "source": "Lambda was unable to configure (?:access to )?your environment variables.{0,800}KMS Exception:\\s*(?:AccessDenied|Disabled|KMSInvalidState|InvalidArn|NotFound)Exception\\b",
          "flags": "i"
        },
        {
          "source": "KMS Exception:\\s*AccessDeniedException\\b.{0,240}ciphertext refers to a customer master key that does not exist,\\s*does not exist in this region,\\s*or you are not allowed to access\\.?",
          "flags": "i"
        }
      ],
      "parse_denial_context": true,
      "parse_stabilization_context": false
    },
    {
      "id": "iam.role.inline-policy-size-limit",
      "title": "An IAM role's inline policies exceed the aggregate size limit",
      "confidence": "high",
      "patterns": [
        {
          "source": "Maximum policy size of 10240 bytes exceeded for (?:the )?role\\b",
          "flags": "i"
        }
      ],
      "explanation": "IAM rejected the role because the combined text of its inline policies exceeded the hard per-role limit. The 10,240-character limit applies to the aggregate inline-policy content on one role, excluding whitespace; it is different from the size of one customer-managed policy and from the adjustable count of managed-policy attachments.",
      "verification": [
        "Inspect the synthesized or packaged template first to see every inline policy that will be attached to the role; the last failed resource may not contain the whole aggregate.",
        "For an existing role, confirm the hard limit and inventory its inline policy names with `aws iam get-account-summary --query 'SummaryMap.RolePolicySizeQuota' --output text` and `aws iam list-role-policies --role-name <role>`.",
        "Read a named policy only after identifying the role and policy with `aws iam get-role-policy --role-name <role> --policy-name <inline-policy>`; these calls are read-only.",
        "Remove duplicate statements and repeated resources while preserving the original permissions. Move a genuinely reusable permission set to a customer-managed policy when that makes ownership clearer, while accounting separately for managed-policy attachment limits.",
        "Do not replace specific actions or resources with broad wildcards to save characters, and do not request a quota increase: this aggregate inline-policy limit is hard."
      ],
      "documentation_url": "https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_iam-quotas.html#reference_iam-limits-entity-length",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "iam.role.managed-policy-attachment-limit",
      "title": "An IAM role reached its managed-policy attachment quota",
      "confidence": "high",
      "patterns": [
        {
          "source": "(?:\\bAttachRolePolicy\\b|\\bLimitExceeded\\b).*Cannot exceed quota for PoliciesPerRole: \\d+",
          "flags": "i"
        },
        {
          "source": "Cannot exceed quota for PoliciesPerRole: \\d+.*(?:\\bServiceLimitExceeded\\b|\\bLimitExceeded\\b)",
          "flags": "i"
        }
      ],
      "explanation": "IAM rejected an AttachRolePolicy call (or the matching CloudFormation resource handler) because attaching another managed policy would exceed the role's `PoliciesPerRole` quota. The emitted quota value is the account's applied limit and can vary. This is separate from the inline-policy document-size limit (#21) and from a single customer-managed policy's document-size quota.",
      "verification": [
        "Confirm the named role and its current attachments with the read-only call `aws iam list-attached-role-policies --role-name <role>`.",
        "Confirm the applied quota with `aws iam get-account-summary --query 'SummaryMap.AttachedPoliciesPerRoleQuota' --output text`.",
        "Remove an attachment only after confirming it is obsolete and who owns it; do not detach policies blindly.",
        "Consolidate policies only when the combined policy stays least-privilege and reviewable; do not broaden actions or resources just to lower the count.",
        "If every attachment is intentional, request an increase for the adjustable IAM quota through Service Quotas (US East (N. Virginia))."
      ],
      "documentation_url": "https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_iam-quotas.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "iam.tag.action-denied",
      "title": "AWS denied a tagging action required by the deployment",
      "confidence": "medium",
      "patterns": [
        {
          "source": "not authorized to perform:?\\s*[a-z0-9]+(?:-[a-z0-9]+)*:(?:(?:Tag|Untag)[a-z0-9]+|(?:Create|Delete)Tags|AddTagsToResource|RemoveTagsFromResource)(?![a-z0-9*])",
          "flags": "i"
        },
        {
          "source": "TagPolicyViolation",
          "flags": "i"
        }
      ],
      "explanation": "The denied IAM action mutates tags. During a create or update, CloudFormation and AWS services often apply tags in the same operation, so a missing paired `TagResource` or `CreateTags` grant can fail the resource operation. Direct retag and untag calls produce the same diagnosis. An AWS Organizations tag policy or a CloudFormation hook can reject the tag set from a layer the member account cannot see.",
      "verification": [
        "Record the exact tagging action, resource, and caller named in the error before changing any policy.",
        "If the called operation was a create or update, grant its paired tagging action: for example `application-autoscaling:TagResource` with `RegisterScalableTarget`, `iam:TagRole` with `CreateRole`, or EC2 `CreateTags` for create-time tags. For a direct retag or untag call, grant only the exact denied mutation.",
        "Scope the permission to the affected resource and, where the service supports them, constrain allowed keys with `aws:RequestTag/${TagKey}` and `aws:TagKeys` conditions.",
        "If an Organizations tag policy or a CloudFormation hook rejected the tag set, identify the layer that enforced it and resolve it with that layer's owner - do not remove a tag policy or hook to make a deployment pass.",
        "Confirm the grant with the IAM Policy Simulator, or look the denied call up in CloudTrail when the error carries a request ID."
      ],
      "documentation_url": "https://docs.aws.amazon.com/IAM/latest/UserGuide/id_tags.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": true,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.tag.key-validation-failed",
      "title": "A tag key or value was rejected by validation",
      "confidence": "medium",
      "patterns": [
        {
          "source": "validation error.{0,120}?'tags\\.\\d{1,3}\\.member\\.(?:key|value)'",
          "flags": "i"
        },
        {
          "source": "'tags\\.\\d{1,3}\\.member\\.(?:key|value)'\\s*failed to satisfy constraint",
          "flags": "i"
        },
        {
          "source": "[Tt]ag key.{0,60}?(?:reserved|cannot begin with|must not begin with).{0,20}?aws:",
          "flags": "i"
        }
      ],
      "explanation": "A tag key or value was rejected before the resource was touched, so this is a template problem rather than a permissions one. The usual causes are a key using the reserved `aws:` prefix, which is reserved for AWS-generated tags and cannot be set, or a key or value that breaks the allowed character set or length. The index in `tags.N.member.key` points at which tag in the submitted set failed.",
      "verification": [
        "Read the index in the error (`tags.N.member.key`) and find the matching tag in the template or `samconfig.toml` tag list.",
        "Rename any key using the reserved `aws:` prefix; that prefix is reserved for AWS and cannot be applied by a deployment.",
        "Check the key and value against the tag restrictions - length limits and the allowed character set - and re-run the deployment."
      ],
      "documentation_url": "https://docs.aws.amazon.com/general/latest/gr/aws_tagging.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "s3.artifact-bucket.access-denied",
      "title": "The deployment bucket denied access to the packaged artifacts",
      "confidence": "high",
      "patterns": [
        {
          "source": "S3 error: Access ?Denied",
          "flags": "i"
        },
        {
          "source": "AccessDenied.{0,160}when calling the (?:PutObject|GetObject|HeadObject|CreateMultipartUpload|UploadPart) operation",
          "flags": "i"
        },
        {
          "source": "(?:Error|Failed) uploading to \\S+.{0,200}Access ?Denied",
          "flags": "i"
        }
      ],
      "explanation": "S3 refused access to the deployment's artifact bucket. The two directions fail for different reasons and need different fixes. Upload (the SAM CLI writing the packaged template and code zips) shows as a denied `PutObject`/`CreateMultipartUpload` while uploading to the named bucket: the deploy identity lacks `s3:PutObject`, a bucket policy or Block Public Access setting denies it, or `s3_bucket` in `samconfig.toml` points at a bucket in another Region or another account. Readback (CloudFormation fetching what was uploaded) shows as `S3 error: Access Denied` while the change set is created: the CloudFormation execution identity - not the CLI's - lacks `s3:GetObject` on the key, or the bucket is SSE-KMS encrypted and that identity has no permission on the key. This is a bucket or key access problem, not an invalid bucket name and not a name collision.",
      "verification": [
        "Note which direction failed - a denied `PutObject` is the CLI uploading, `S3 error: Access Denied` during change-set creation is CloudFormation reading back - and confirm the caller with `aws sts get-caller-identity`.",
        "Run `aws s3api head-object --bucket <deploy-bucket> --key <key>` (read-only): success means the upload landed and the readback identity is the one being denied.",
        "Run `aws s3api get-bucket-location --bucket <deploy-bucket>` and compare it with the deployment Region; a wrong-Region `s3_bucket` in `samconfig.toml` surfaces as a denial.",
        "Run `aws s3api get-bucket-encryption --bucket <deploy-bucket>`: if it uses SSE-KMS, the denied identity also needs `kms:Decrypt` (and `kms:GenerateDataKey` to upload) on that key.",
        "Confirm who owns the bucket before changing any policy - for a cross-account bucket, pass `--expected-bucket-owner` when checking, and fix the bucket policy in the owning account rather than broadening the deploy role."
      ],
      "documentation_url": "https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/sam-cli-command-reference-sam-deploy.html",
      "suppressed_by": [
        {
          "source": "(?:access has been denied by S3[\\s\\S]*permission[\\s\\S]*GetObject)",
          "flags": "i"
        },
        {
          "source": "permission to GetObject for.*bucket",
          "flags": "i"
        }
      ],
      "excluded_line_patterns": [
        {
          "source": "is not authorized to perform:",
          "flags": "i"
        }
      ],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "aws.credentials.expired",
      "title": "The AWS credentials used by the deployment have expired",
      "confidence": "high",
      "patterns": [
        {
          "source": "ExpiredToken(?:Exception)?",
          "flags": "i"
        },
        {
          "source": "security token included in the request is expired",
          "flags": "i"
        },
        {
          "source": "Signature expired:.*is now earlier than",
          "flags": "i"
        }
      ],
      "explanation": "AWS rejected the request because the temporary credentials were no longer valid when it arrived. This is a credential-lifetime or clock problem, not a permissions or template problem: the session expired (cached SSO login, long-lived shell, short OIDC session duration), or the machine's clock is far enough off that the request signature is treated as stale.",
      "verification": [
        "Refresh the credentials for the environment that failed: re-run `aws sso login`, rotate the CI secret, or re-assume the role; for OIDC deployments check the role's maximum session duration.",
        "For a `Signature expired ... is now earlier than` message, compare the two timestamps in the error: a gap of more than a few minutes means the runner's clock is skewed, so sync it (NTP) rather than rotating credentials.",
        "Confirm the deployment picks up the refreshed credentials (no stale AWS_* environment variables or cached profiles) before retrying."
      ],
      "documentation_url": "https://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_temp_request.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "aws.credentials.invalid",
      "title": "The deployment ran with invalid or wrong-account AWS credentials",
      "confidence": "high",
      "patterns": [
        {
          "source": "security token included in the request is invalid",
          "flags": "i"
        },
        {
          "source": "UnrecognizedClientException",
          "flags": "i"
        }
      ],
      "explanation": "The request never passed authentication: the security token is invalid, or the access key belongs to no known account. This is not the same failure as expired credentials - AWS rejected the token itself rather than a token that was once valid. Common causes are stale `AWS_*` environment variables overriding the intended profile, credentials for the wrong account, or a region configured under only one `samconfig.toml` section.",
      "verification": [
        "Run `aws configure list` in the failing environment to see which source (environment variable, profile, or IAM role) supplied each credential value.",
        "Check for `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, or `AWS_SESSION_TOKEN` environment variables overriding the intended profile.",
        "Once credentials are corrected, confirm the account with `aws sts get-caller-identity`."
      ],
      "documentation_url": "https://docs.aws.amazon.com/cli/latest/userguide/cli-configure-envvars.html",
      "suppressed_by": [
        {
          "source": "ExpiredToken(?:Exception)?",
          "flags": "i"
        },
        {
          "source": "security token included in the request is expired",
          "flags": "i"
        }
      ],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.api.throttled",
      "title": "CloudFormation throttled the deployment's API calls",
      "confidence": "medium",
      "patterns": [
        {
          "source": "An error occurred \\(Throttling\\)",
          "flags": "i"
        },
        {
          "source": "\\bRate exceeded\\b",
          "flags": "i"
        }
      ],
      "explanation": "AWS rejected API calls because the account exceeded a request rate limit in this Region. This is a transient capacity condition, not a template or permission problem; the same deployment can succeed on retry once the call rate drops.",
      "verification": [
        "Retry the deployment after a pause; if this recurs, add backoff or retries around the deploy step instead of changing the template.",
        "Reduce the number of parallel stack operations from CI (matrix jobs, monorepo fan-out) targeting the same account and Region.",
        "Check for other automation (drift detection, scheduled deployments, dashboards polling DescribeStacks) hammering CloudFormation in the same account and Region, and request a quota increase only if the steady-state rate is genuinely higher."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/cloudformation-limits.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "apigateway.control-plane.throttled",
      "title": "API Gateway throttled a deployment control-plane request",
      "confidence": "medium",
      "patterns": [
        {
          "source": "An error occurred \\(TooManyRequestsException\\) when calling the (?:CreateApi|CreateDeployment|CreateRestApi|CreateStage|CreateUsagePlan|ImportApi|ImportRestApi|PutRestApi|ReimportApi|UpdateUsagePlan) operation\\b",
          "flags": "i"
        },
        {
          "source": "\\b(?:CREATE_FAILED|UPDATE_FAILED)\\b.{0,1000}Too Many Requests\\s*\\(Service:\\s*ApiGateway,\\s*Status Code:\\s*429\\b",
          "flags": "i"
        }
      ],
      "explanation": "API Gateway rejected an API create, deploy, or update request because the account exceeded a control-plane request-rate quota in this Region. This 429 is normally transient and is not evidence that the template is invalid. Tight or unbounded retries add request pressure and can prolong the throttling.",
      "verification": [
        "Preserve the named operation, account, and Region, then retry after a pause with exponential backoff and jitter; remove immediate or unbounded CI retry loops.",
        "Serialize or reduce API-creating and API-updating deployments that target the same account and Region.",
        "Check Amazon API Gateway in Service Quotas and the API Gateway control-plane quota table for the failing Region; request an increase only when the quota is listed as adjustable, otherwise reduce the steady request rate."
      ],
      "documentation_url": "https://docs.aws.amazon.com/apigateway/latest/developerguide/limits.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "sam.template.invalid-property",
      "title": "A SAM template property is not valid for its resource type",
      "confidence": "high",
      "patterns": [
        {
          "source": "property\\s+\\S+:\\s+not defined for resource of type AWS::Serverless::",
          "flags": "i"
        }
      ],
      "explanation": "SAM rejected a property key that is not defined for the resource type. This is commonly a misspelling, punctuation error in the key, or a property copied from a different SAM or CloudFormation resource.",
      "verification": [
        "Compare the exact property key in the template with the reference for the resource type named in the error.",
        "Check for punctuation accidentally included in a YAML or JSON property name before changing related API configuration.",
        "Run `sam validate` after correcting the template shape."
      ],
      "documentation_url": "https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/sam-resource-api.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "sam.validate.lint-rule-matched",
      "title": "SAM lint validation found one or more cfn-lint matches",
      "confidence": "medium",
      "patterns": [
        {
          "source": "Linting failed\\. At least one linting rule was matched to the provided template\\.",
          "flags": "i"
        }
      ],
      "explanation": "SAM finished lint validation with at least one cfn-lint match. This summary line does not identify the template problem; the nearby E- or W-prefixed rule output names the rule and location that need review.",
      "verification": [
        "Re-run `sam validate --lint` against the exact submitted template and read the E- or W-prefixed rule output immediately before the summary line.",
        "Use `cfn-lint --list-rules` or the cfn-lint rule catalog to interpret each reported rule code instead of inferring a cause from this summary.",
        "After reviewing the reported template locations, run `sam validate --lint` again and confirm that no lint matches remain before deploying."
      ],
      "documentation_url": "https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/validate-cfn-lint.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "sam.template.schema-validation-failed",
      "title": "The template failed SAM or CloudFormation schema validation",
      "confidence": "high",
      "patterns": [
        {
          "source": "InvalidSamDocumentException",
          "flags": "i"
        },
        {
          "source": "InvalidResourceException",
          "flags": "i"
        },
        {
          "source": "Encountered unsupported property",
          "flags": "i"
        },
        {
          "source": "property\\s+\\S+\\s+not defined for resource of type",
          "flags": "i"
        }
      ],
      "explanation": "SAM or CloudFormation rejected the template before creating any resource: the document failed schema validation, or a resource type does not recognize one of its properties. This is commonly an indentation mistake, a property nested at the wrong level, or a missing `Transform: AWS::Serverless-2016-10-31` line.",
      "verification": [
        "Run `sam validate --lint` locally and in CI before deploying.",
        "Compare the resource id and property path named in the error with the reference for that resource type.",
        "Confirm the template still has its `Transform: AWS::Serverless-2016-10-31` line when it declares SAM resource types."
      ],
      "documentation_url": "https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/sam-cli-command-reference-sam-validate.html",
      "suppressed_by": [
        {
          "source": "property\\s+\\S+:\\s+not defined for resource of type AWS::Serverless::",
          "flags": "i"
        }
      ],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.template.getatt-parameters-invalid",
      "title": "Fn::GetAtt parameters are malformed",
      "confidence": "high",
      "patterns": [
        {
          "source": "every Fn::GetAtt object requires two non-empty parameters",
          "flags": "i"
        }
      ],
      "explanation": "CloudFormation rejected the template before changing any resources because one `Fn::GetAtt` expression does not contain exactly two non-empty parts: a logical resource id and an attribute name. The error does not identify which occurrence is malformed.",
      "verification": [
        "Search the exact submitted or synthesized template for every `Fn::GetAtt` and `!GetAtt` occurrence.",
        "Write the long form as `Fn::GetAtt: [LogicalResourceId, AttributeName]` or the short form as `!GetAtt LogicalResourceId.AttributeName`.",
        "Validate the corrected template before creating the change set again; for SAM, run `sam validate --lint` against the template you deploy."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/intrinsic-function-reference-getatt.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.template.unresolved-dependency",
      "title": "The template references an unresolved resource dependency",
      "confidence": "high",
      "patterns": [
        {
          "source": "Template format error:\\s*Unresolved resource dependencies\\b",
          "flags": "i"
        }
      ],
      "explanation": "CloudFormation could not resolve one or more logical IDs named in the template. The bracketed names are usually misspelled or missing resources, parameters, or `DependsOn` targets; the change set fails before resource provisioning starts.",
      "verification": [
        "Read the names inside `Unresolved resource dependencies <logical-ids>` and compare each one with the exact logical IDs under `Resources` and `Parameters` in the submitted or SAM-transformed template.",
        "Check `Ref`, `Fn::GetAtt`, `DependsOn`, and substitutions for spelling and case; a logical ID is case-sensitive and must exist in the same template scope.",
        "Run `sam validate --lint` or `cfn-lint` against the exact template that the deploy submits, then retry the change set only after every referenced ID resolves."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/template-anatomy.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.template.circular-dependency",
      "title": "CloudFormation found a circular resource dependency",
      "confidence": "high",
      "patterns": [
        {
          "source": "\\bCircular dependency between resources\\b(?::|$)",
          "flags": "i"
        }
      ],
      "explanation": "CloudFormation found a cycle in the template's resource dependency graph and cannot choose an order that creates or updates the resources. The cycle is usually introduced by implicit `Ref`, `Fn::GetAtt`, or `Fn::Sub` references, or by an explicit `DependsOn`; it fails before resource provisioning and is not an IAM or retry problem.",
      "verification": [
        "Capture the logical IDs listed after `Circular dependency between resources` and compare them with the exact SAM-transformed or CDK-synthesized template.",
        "Trace every `Ref`, `Fn::GetAtt`, `Fn::Sub`, and `DependsOn` edge among those resources in both directions until the cycle is visible.",
        "Break the cycle by removing an unnecessary reference, passing the value through a parameter or separate stack, or moving the dependent resource; add `DependsOn` only for a real one-way ordering requirement, never to a cycle.",
        "Run `sam validate --lint` or `cfn-lint` against the exact artifact the deployment submits, then synthesize again before retrying the change set."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/troubleshooting.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "iam.trust-policy.resource-field-invalid",
      "title": "An IAM role trust policy contains a permissions-only Resource field",
      "confidence": "high",
      "patterns": [
        {
          "source": "Has prohibited field Resource",
          "flags": "i"
        }
      ],
      "explanation": "IAM rejected the role trust policy because it contains a `Resource` field. A role trust policy defines who may assume the role; permissions on AWS resources belong in an identity policy attached to that role.",
      "verification": [
        "Inspect the role's `AssumeRolePolicyDocument` and remove `Resource` or `NotResource` elements from that trust policy.",
        "Keep the trusted principal and `sts:AssumeRole` action in the trust policy.",
        "Move service permissions to the role's `Policies` or `ManagedPolicyArns`, then review the least-privilege scope."
      ],
      "documentation_url": "https://docs.aws.amazon.com/IAM/latest/UserGuide/access-analyzer-reference-policy-checks.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "lambda.code-signing.image-incompatible",
      "title": "Lambda code signing is incompatible with a container-image function",
      "confidence": "high",
      "patterns": [
        {
          "source": "Code signing is not supported for functions created with container images",
          "flags": "i"
        }
      ],
      "explanation": "Lambda does not support a code signing configuration for a function packaged as a container image. The template must use a supported packaging and signing combination.",
      "verification": [
        "Confirm whether the function uses `PackageType: Image` or an image URI.",
        "Remove the code signing configuration for an image-packaged function, or switch to a signed ZIP deployment package if code signing is required.",
        "Review the function's deployment and integrity requirements before changing package type."
      ],
      "documentation_url": "https://docs.aws.amazon.com/lambda/latest/dg/configuration-codesigning-create.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "s3.bucket-name.invalid",
      "title": "An S3 bucket name failed AWS validation",
      "confidence": "high",
      "patterns": [
        {
          "source": "Error Code:\\s*InvalidBucketName",
          "flags": "i"
        },
        {
          "source": "The specified bucket is not valid",
          "flags": "i"
        }
      ],
      "explanation": "S3 rejected the bucket name before deployment could continue. The configured or generated name violates S3 naming rules; this is distinct from a bucket that already exists or a missing permission.",
      "verification": [
        "Inspect the configured deployment bucket and any environment-derived suffix used to construct its name.",
        "Check for uppercase letters, underscores, invalid length, adjacent periods, an IP-address-like form, or invalid leading and trailing characters.",
        "Use a unique, lowercase bucket name that satisfies the documented S3 naming rules."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AmazonS3/latest/userguide/bucketnamingrules.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "s3.bucket-name.already-taken",
      "title": "An S3 bucket name in the template is already taken",
      "confidence": "high",
      "patterns": [
        {
          "source": "BucketAlreadyExists",
          "flags": "i"
        },
        {
          "source": "BucketAlreadyOwnedByYou",
          "flags": "i"
        },
        {
          "source": "already exists\\s*\\(Service:\\s*S3\\b",
          "flags": "i"
        },
        {
          "source": "The requested bucket name is not available",
          "flags": "i"
        }
      ],
      "explanation": "S3 bucket names are globally unique across every AWS account, so the explicit `BucketName` the stack asked for could not be created. `BucketAlreadyExists` means some other account already holds the name; `BucketAlreadyOwnedByYou` means this account already has the bucket, usually left behind by a deleted or renamed stack. Newer resource handlers report the same collision as \"The requested bucket name is not available\" with `HandlerErrorCode: AlreadyExists`. This is a name collision, not an invalid name and not a missing permission.",
      "verification": [
        "Read the failed `AWS::S3::Bucket` event and record the exact bucket name the stack requested.",
        "For `BucketAlreadyExists`, pick a name that is unique globally - add an account or environment suffix - or omit `BucketName` entirely and let CloudFormation generate one.",
        "For `BucketAlreadyOwnedByYou`, confirm the leftover bucket with `aws s3api head-bucket --bucket <name>` (read-only) and check its contents before reusing the name; import the existing bucket into the stack or rename, and do not delete a bucket you have not inspected."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AmazonS3/latest/userguide/bucketnamingrules.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "s3.lifecycle.abort-multipart-tag-filter",
      "title": "An S3 lifecycle abort rule cannot use a tag filter",
      "confidence": "high",
      "patterns": [
        {
          "source": "AbortIncompleteMultipartUpload cannot be specified with Tags",
          "flags": "i"
        }
      ],
      "explanation": "S3 rejected a lifecycle rule because `AbortIncompleteMultipartUpload` cannot be combined with a tag-based filter. Incomplete multipart uploads do not carry object tags yet, so S3 cannot evaluate that filter for the abort action.",
      "verification": [
        "Find the lifecycle rule on the affected bucket and separate the multipart-abort action from any tag-filtered expiration or transition rule.",
        "Keep the abort rule unfiltered or scope it with an object-key prefix; use a separate rule for tag-based object actions.",
        "Read the current configuration before editing it with `aws s3api get-bucket-lifecycle-configuration --bucket <bucket>` (read-only)."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AmazonS3/latest/userguide/lifecycle-configuration-examples.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "imagebuilder.recipe.version-already-exists",
      "title": "An EC2 Image Builder recipe version already exists",
      "confidence": "high",
      "patterns": [
        {
          "source": "(?:following )?resource\\s+['\\\"]?ImageRecipe['\\\"]?\\s+already exists",
          "flags": "i"
        },
        {
          "source": "ResourceAlreadyExistsException.*(?:ImageRecipe|CreateImageRecipe)",
          "flags": "i"
        },
        {
          "source": "(?:ImageRecipe|CreateImageRecipe).*ResourceAlreadyExistsException",
          "flags": "i"
        }
      ],
      "explanation": "EC2 Image Builder recipes are immutable after creation. The deployment is trying to create an image recipe with a name and semantic version that already exists, often because the recipe changed without a version bump or a previous stack left that version behind.",
      "verification": [
        "Read the image recipe ARN, name, and semantic version from the failed event, then inspect it with `aws imagebuilder get-image-recipe --image-recipe-arn <recipe-arn>` (read-only).",
        "If the recipe content changed, create a new semantic version instead of reusing the existing one; Image Builder also supports an `x` wildcard for automatic version increments.",
        "Check the CloudFormation stack's replacement behavior and any image pipeline or distribution that still references the old recipe before removing or retiring it."
      ],
      "documentation_url": "https://docs.aws.amazon.com/imagebuilder/latest/userguide/manage-recipes.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.api.service-unavailable",
      "title": "CloudFormation was temporarily unavailable",
      "confidence": "high",
      "patterns": [
        {
          "source": "An error occurred \\((?:ServiceNotAvailable|ServiceUnavailable)\\) when calling the (?:CreateStack|UpdateStack|CreateChangeSet|ExecuteChangeSet) operation",
          "flags": "i"
        },
        {
          "source": "(?:ServiceUnavailableException|ServiceNotAvailable).{0,160}(?:CloudFormation|CreateStack|UpdateStack|CreateChangeSet|ExecuteChangeSet)",
          "flags": "i"
        }
      ],
      "explanation": "CloudFormation returned a temporary service-unavailable response before the stack operation could complete. This is an AWS control-plane outage or interruption, not evidence that the template or IAM policy is wrong.",
      "verification": [
        "Check AWS Health or the regional CloudFormation service status for an active interruption.",
        "Wait briefly and retry with exponential backoff. For a new stack, keep a stable `--client-request-token` so a retry is not mistaken for a second create if AWS received the first request.",
        "If the error repeats after the service recovers, preserve the exact operation, Region, timestamp, and request ID before opening an AWS support case; do not change the template based on this response alone."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/troubleshooting.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.deploy.wrapper-failed",
      "title": "The CloudFormation deploy wrapper reported a failure without the root cause",
      "confidence": "low",
      "patterns": [
        {
          "source": "Failed to create/update (?:the )?stack\\b",
          "flags": "i"
        }
      ],
      "explanation": "This is the SAM or CloudFormation deploy wrapper reporting that the operation failed; it is not the resource-level cause. The useful status reason is in the stack events, often a few lines earlier or only available through the read-only events command.",
      "verification": [
        "Fetch the failed stack's events with `aws cloudformation describe-stack-events --stack-name <stack>` (read-only).",
        "Inspect the earliest `CREATE_FAILED` or `UPDATE_FAILED` event and preserve its exact status reason; that is the evidence to run through SAM Doctor.",
        "If the event stream has no resource failure, keep the operation, Region, timestamp, and request ID and investigate the surrounding API error instead of changing the template based on this wrapper line."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/view-stack-events.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cdk.asset.bundling-failed",
      "title": "AWS CDK could not bundle an asset before deployment",
      "confidence": "low",
      "patterns": [
        {
          "source": "Failed to bundle asset\\b.{0,2000}\\bbundle output is located at\\b",
          "flags": "i"
        }
      ],
      "explanation": "AWS CDK could not build a local asset such as Lambda code before it could synthesize or deploy the stack. The bundle wrapper names the asset and temporary output directory, but the useful compiler, dependency, permission, or Docker error is usually in the text after the `-error` suffix or in the preceding command output.",
      "verification": [
        "Rerun `cdk synth --verbose` from the same project directory with the same app, context, and environment, then inspect the first bundler error for the named asset.",
        "Check the asset's package manager dependencies and build command in the failing directory; for Docker bundling, confirm the runner can execute the exact image and command before changing the generated template.",
        "If this came from an Amplify or another CDK-backed wrapper, preserve the complete underlying bundler error and fix the source or build environment before retrying the backend deployment."
      ],
      "documentation_url": "https://docs.aws.amazon.com/cdk/v2/guide/assets.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cdk.synth.assembly-failed",
      "title": "AWS CDK reported an assembly failure without the underlying error",
      "confidence": "low",
      "patterns": [
        {
          "source": "(?:\\bAssemblyError:\\s*Assembly builder failed\\b|\\[_AssemblyError\\]\\s*Assembly builder failed\\b)",
          "flags": "i"
        }
      ],
      "explanation": "The CDK Toolkit reported that its assembly builder failed, but this wrapper line does not identify the application, dependency, or validation error that stopped synthesis. The useful error is usually earlier in the log or appears when synthesis is rerun with the same app and context.",
      "verification": [
        "Rerun `cdk synth --verbose` from the same project directory with the same app, context, and environment as the failed deploy.",
        "Inspect the first preceding error or exception and preserve its complete message; check the app's dependencies and CDK context before changing the generated template.",
        "If synthesis succeeds but deployment later fails, keep the CloudFormation resource event and diagnose that more specific status reason instead of this wrapper line."
      ],
      "documentation_url": "https://docs.aws.amazon.com/cdk/v2/guide/ref-cli-cmd-synth.html",
      "suppressed_by": [
        {
          "source": "Failed to bundle asset\\b.{0,2000}\\bbundle output is located at\\b",
          "flags": "i"
        }
      ],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "lambda.invoke.function-not-found",
      "title": "Lambda invoke targeted a function or qualifier that was not found",
      "confidence": "medium",
      "patterns": [
        {
          "source": "ResourceNotFoundException\\b.{0,160}\\bwhen calling (?:the )?Invoke(?: operation)?\\b",
          "flags": "i"
        },
        {
          "source": "\\bwhen calling (?:the )?Invoke(?: operation)?\\b.{0,160}ResourceNotFoundException\\b",
          "flags": "i"
        }
      ],
      "explanation": "A Lambda `Invoke` call reached AWS, but the requested function, alias, or published version was not found in the account and Region used by the caller. This is often a post-deploy smoke test running before the function or alias is available, or a command using the wrong name, qualifier, account, or Region; the message alone does not prove that the CloudFormation deployment itself failed.",
      "verification": [
        "Confirm the function name, qualifier, account, and Region in the invoke command, then read its presence with `aws lambda get-function --function-name <function-name> --qualifier <alias-or-version>` (omit `--qualifier` when none was requested).",
        "If the invoke follows a deployment, wait for the stack and any `AutoPublishAlias` or version resource to complete, then read the available aliases and versions before retrying.",
        "Compare the invoke target with the stack output or transformed template; correct a stale function name, alias, Region, or account rather than broadening IAM permissions for a missing target."
      ],
      "documentation_url": "https://docs.aws.amazon.com/lambda/latest/api/API_Invoke.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "bedrock.model-lifecycle.end-of-life",
      "title": "The Bedrock model version has reached end of life",
      "confidence": "high",
      "patterns": [
        {
          "source": "(?:Bedrock Runtime|bedrock-runtime).{0,500}ResourceNotFoundException\\b.{0,160}This model version has reached the end of its life\\b",
          "flags": "i"
        },
        {
          "source": "\\bThis model version has reached the end of its life\\b",
          "flags": "i"
        }
      ],
      "explanation": "Amazon Bedrock rejected the invocation because the requested model version is no longer available. This is a model-lifecycle migration problem, not a missing permission or a transient deployment failure.",
      "verification": [
        "Record the exact model ID and Region from the evidence, then check its lifecycle status with `aws bedrock get-foundation-model --model-identifier <model-id>` (read-only).",
        "Select an Active model version supported by the same Region and API, update the application or deployment configuration, and test the new model's request shape before retrying.",
        "Do not retry the retired version or broaden IAM permissions; Bedrock does not migrate model IDs automatically after end of life."
      ],
      "documentation_url": "https://docs.aws.amazon.com/bedrock/latest/userguide/model-lifecycle.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "bedrock.model-access.first-use-form-required",
      "title": "Bedrock model access has not been submitted",
      "confidence": "medium",
      "patterns": [
        {
          "source": "ResourceNotFoundException\\b.{0,220}\\bModel use case details have not been submitted for this account\\b",
          "flags": "i"
        },
        {
          "source": "Model use case details have not been submitted for this account\\b",
          "flags": "i"
        }
      ],
      "explanation": "Amazon Bedrock rejected the model call because the account has not submitted the model provider's first-use details. This is an account and model-access gate, not evidence that a CloudFormation resource is missing; a post-deploy smoke test can surface it alongside a deployment.",
      "verification": [
        "Confirm the account, Region, and model ID used by the failing call, then open the Bedrock model-access or first-use form for that provider in the same account.",
        "For Anthropic models, submit the Anthropic use-case details form requested by Bedrock and wait for the account's access state to update before retrying.",
        "Retry the same model call after access is granted; do not delete the stack or broaden IAM permissions solely because this ResourceNotFoundException appeared."
      ],
      "documentation_url": "https://docs.aws.amazon.com/bedrock/latest/userguide/troubleshooting-api-error-codes.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "bedrock.model-identifier.unresolved",
      "title": "Bedrock could not resolve the model identifier",
      "confidence": "medium",
      "patterns": [
        {
          "source": "Could not resolve the foundation model from the provided model identifier\\b",
          "flags": "i"
        }
      ],
      "explanation": "Amazon Bedrock could not resolve the model identifier at the runtime endpoint. The identifier may be retired, unavailable in the selected Region, for a different API, or missing the inference-profile prefix; this message is a model lookup failure, not an IAM denial.",
      "verification": [
        "Record the exact model ID and Region from the failing request, then compare them with the current Bedrock model catalog and the model's supported API and Region.",
        "Use the current base-model ID, inference-profile ID, provisioned-model ARN, or custom-model identifier for the way the model is hosted; do not substitute a display name.",
        "Retry after correcting the identifier or Region, and check the model lifecycle page if the configured version was retired."
      ],
      "documentation_url": "https://docs.aws.amazon.com/bedrock/latest/userguide/models.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "bedrock.request.empty-system-prompt",
      "title": "Bedrock received an empty system prompt",
      "confidence": "medium",
      "patterns": [
        {
          "source": "Invalid length for parameter system\\[\\d+\\]\\.text\\b",
          "flags": "i"
        }
      ],
      "explanation": "The Bedrock Converse request was rejected during client-side parameter validation because a system content block has empty text. Bedrock's system block requires at least one character, so this is a request shape problem before model access or IAM is evaluated.",
      "verification": [
        "Inspect the request builder that creates the Converse or ConverseStream payload and omit the system field when the generated prompt is empty or whitespace-only.",
        "Keep non-empty system instructions as text blocks, then log or test the serialized request shape without recording credentials or user content.",
        "Retry the same model call after removing the empty block; only then investigate separate model-access or identifier errors."
      ],
      "documentation_url": "https://docs.aws.amazon.com/bedrock/latest/APIReference/API_runtime_SystemContentBlock.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "bedrock.request.empty-model-id",
      "title": "The Bedrock InvokeModel request omitted modelId",
      "confidence": "medium",
      "patterns": [
        {
          "source": "(?:(?:operation error Bedrock Runtime:\\s*InvokeModel(?:WithResponseStream)?|when calling (?:the )?InvokeModel(?:WithResponseStream)? operation).{0,260})?\\binput member modelId must not be empty\\b",
          "flags": "i"
        }
      ],
      "explanation": "The Bedrock Runtime client rejected the request before inference because the required `modelId` value was empty. This is a request configuration or serialization problem, not evidence that the model is unavailable or that IAM denied `bedrock:InvokeModel`.",
      "verification": [
        "Inspect the code or configuration that selects the model and find why the value became an empty string; check environment-variable names, backend selection, and default values first.",
        "Set `modelId` to the exact model ID, inference-profile ID, or supported ARN for the target Region, then log only the non-sensitive request shape before retrying.",
        "If the application supports both InvokeModel and Converse, verify that the selected backend passes the model identifier to the operation it actually calls; do not change IAM or model access until a non-empty identifier reaches the SDK."
      ],
      "documentation_url": "https://docs.aws.amazon.com/bedrock/latest/APIReference/API_runtime_InvokeModel.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "bedrock.request.messages-required",
      "title": "The Bedrock request omitted the required messages field",
      "confidence": "medium",
      "patterns": [
        {
          "source": "(?:operation error Bedrock Runtime:\\s*InvokeModel(?:WithResponseStream)?|when calling (?:the )?InvokeModel(?:WithResponseStream)? operation|Bedrock\\b.{0,120}\\bValidationException\\b).{0,260}\\bmessages:\\s*Field required\\b",
          "flags": "i"
        }
      ],
      "explanation": "Bedrock rejected the model-specific request body because the required `messages` field was missing. This is a request-shape or model/API compatibility problem, not evidence that the model is unavailable or that IAM denied the invocation.",
      "verification": [
        "Confirm the selected model supports the Anthropic Claude Messages API, then inspect the serialized `InvokeModel` body for the required `messages` array; log only field names and sanitized values.",
        "For the Claude Messages API, include `anthropic_version` set to `bedrock-2023-05-31`, a positive `max_tokens`, and at least the required user or assistant message shape with role and content.",
        "Do not send an OpenAI-style `prompt` body to a model expecting Messages API fields; if the model supports Converse, use its `messages` and `system` shape instead and verify the selected operation matches the body format.",
        "Retry after correcting the request body, then investigate model access or IAM only if Bedrock reports a separate service-side error."
      ],
      "documentation_url": "https://docs.aws.amazon.com/bedrock/latest/userguide/model-parameters-anthropic-claude-messages-request-response.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "bedrock.request.message-content-field-required",
      "title": "A Bedrock message content block is missing a required field",
      "confidence": "medium",
      "patterns": [
        {
          "source": "\\bmessages\\.\\d+\\.content\\.\\d+\\.[A-Za-z][A-Za-z0-9_.]*:\\s*Field required\\b",
          "flags": "i"
        }
      ],
      "explanation": "Bedrock's model-specific validation named a missing field inside a particular message content block. The indexed path identifies the turn, content block, and nested field to inspect; this is a request shape problem, not evidence that model access or IAM is wrong.",
      "verification": [
        "Read the indexed path such as `messages.1.content.0.thinking.signature` or `messages.0.content.1.image.source` and inspect that exact block in the serialized request.",
        "Match the content block type to the model's Messages API schema: text blocks need text, thinking blocks need their signed fields when replayed, and image or document blocks need the required source object and type.",
        "If an adapter converts between Anthropic Messages, Bedrock InvokeModel, and Converse, log only the sanitized content-block types and keys to find the field that was dropped or renamed.",
        "Retry after correcting the nested block, then investigate model access or IAM only if a separate service-side error remains."
      ],
      "documentation_url": "https://docs.aws.amazon.com/bedrock/latest/userguide/model-parameters-anthropic-claude-messages-request-response.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "aws.api.action-invalid",
      "title": "The AWS endpoint rejected the requested API action",
      "confidence": "low",
      "patterns": [
        {
          "source": "(?:UnknownAction|InvalidAction)\\b.{0,120}\\bwhen calling\\b",
          "flags": "i"
        },
        {
          "source": "\\bwhen calling\\b.{0,120}\\b(?:UnknownAction|InvalidAction)\\b",
          "flags": "i"
        }
      ],
      "explanation": "The endpoint rejected the requested AWS API action as unknown or invalid. The operation may be misspelled, unavailable in the service API version or Region, unsupported by a local emulator, or being sent to the wrong endpoint; the line does not establish an IAM problem.",
      "verification": [
        "Read the operation name and endpoint from the evidence line, then compare them with the current service API and the Region selected by the caller.",
        "Update the AWS CLI or SDK, correct the operation or endpoint, or check whether the local emulator implements that action before changing deployment code.",
        "Retry the same call after the operation is supported; do not grant broader permissions for an action the endpoint does not recognize."
      ],
      "documentation_url": "https://docs.aws.amazon.com/ec2/latest/devguide/errors-overview.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "aws.api.action-not-implemented",
      "title": "The endpoint does not implement the requested API action",
      "confidence": "low",
      "patterns": [
        {
          "source": "NotImplemented\\b.{0,120}\\bwhen calling\\b",
          "flags": "i"
        },
        {
          "source": "\\bwhen calling\\b.{0,120}\\bNotImplemented\\b",
          "flags": "i"
        }
      ],
      "explanation": "The endpoint returned a NotImplemented response for the requested operation. The selected service endpoint, API version, proxy, or local emulator may not support that operation or request shape; the line does not establish an IAM problem or prove that AWS lacks the operation everywhere.",
      "verification": [
        "Read the operation, service, endpoint, and HTTP status from the exact evidence line, then compare the operation with the current service API reference.",
        "Check whether a custom endpoint, proxy, emulator, or stale SDK is handling the request; update the client or use a supported API operation when appropriate.",
        "Retry against the intended AWS Region and endpoint after correcting the compatibility issue; do not grant broader permissions for an operation the endpoint cannot implement."
      ],
      "documentation_url": "https://docs.aws.amazon.com/scheduler/latest/APIReference/API_ListSchedules.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "aws.api.service-unknown",
      "title": "The endpoint could not route the request to a known AWS service",
      "confidence": "low",
      "patterns": [
        {
          "source": "UnknownService\\b.{0,160}\\bwhen calling\\b",
          "flags": "i"
        },
        {
          "source": "\\bwhen calling\\b.{0,160}\\bUnknownService\\b",
          "flags": "i"
        }
      ],
      "explanation": "The request reported an UnknownService marker while calling an AWS operation. A custom endpoint, proxy, emulator, service target, or client protocol may not be registered or may not match the operation; the line does not establish an IAM problem or prove that AWS lacks the service.",
      "verification": [
        "Preserve the service, operation, endpoint URL, Region, SDK or CLI version, and protocol target from the same evidence line.",
        "Confirm that the endpoint is intended for that AWS service and that any proxy or emulator registers the service and wire protocol used by the client.",
        "Retry against the intended AWS endpoint or update the client or emulator after correcting the routing mismatch; do not grant broader permissions for an endpoint that cannot identify the service."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AmazonCloudWatch/latest/APIReference/API_PutMetricData.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "aws.credentials.caller-identity-unavailable",
      "title": "The deployment could not read the STS caller identity",
      "confidence": "low",
      "patterns": [
        {
          "source": "\\bError:\\s*reading STS Caller Identity\\b",
          "flags": "i"
        },
        {
          "source": "\\boperation error STS:\\s*GetCallerIdentity\\b.{0,240}\\b(?:error|failed|status)\\b",
          "flags": "i"
        }
      ],
      "explanation": "The deployment or provider could not complete the STS GetCallerIdentity check. This wrapper line is not the root cause: the nested response may point to an endpoint, Region, profile, signature, network, or credential-source mismatch. It is not by itself evidence that sts:GetCallerIdentity permission is missing.",
      "verification": [
        "Read the nested STS response, HTTP status, endpoint, and Region from the same log block instead of stopping at the wrapper line.",
        "In the failing environment, run `aws sts get-caller-identity --region <region>` with the same profile, role, endpoint URL, and credential source to identify the account and caller.",
        "Correct the endpoint, Region, signing, network, or credential-source mismatch shown by the nested error; do not add an IAM allow for `sts:GetCallerIdentity`, which does not require permission for this check."
      ],
      "documentation_url": "https://docs.aws.amazon.com/STS/latest/APIReference/API_GetCallerIdentity.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "lambda.vpc.execution-role-network-interface-permission",
      "title": "The Lambda execution role cannot create VPC network interfaces",
      "confidence": "high",
      "patterns": [
        {
          "source": "The provided execution role does not have permission(?:s)? to call CreateNetworkInterface on EC2\\b",
          "flags": "i"
        }
      ],
      "explanation": "Lambda is creating or updating a VPC-connected function, but the function's execution role cannot create the elastic network interfaces Lambda uses for that VPC attachment. This is a role permission problem on the function, not a permission missing from the CloudFormation or SAM deployer.",
      "verification": [
        "Confirm the function has a `VpcConfig` and identify its execution role; do not change the role that runs `sam deploy` or CloudFormation.",
        "Read the execution role's policies and grant the required ENI actions: `ec2:CreateNetworkInterface`, `ec2:DescribeNetworkInterfaces`, `ec2:DescribeSubnets`, `ec2:DeleteNetworkInterface`, `ec2:AssignPrivateIpAddresses`, and `ec2:UnassignPrivateIpAddresses`.",
        "For a managed-policy path, verify `AWSLambdaVPCAccessExecutionRole` is attached to the function's execution role. If the policy is created in the same stack, wait for IAM propagation and preserve the dependency before retrying.",
        "After the role policy is effective, redeploy the function and keep the VPC subnets and security groups unchanged while checking whether the error moves to a separate network or quota failure."
      ],
      "documentation_url": "https://docs.aws.amazon.com/lambda/latest/dg/configuration-vpc.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "ec2.user-data.size-limit-exceeded",
      "title": "EC2 rejected user data larger than 16,384 bytes",
      "confidence": "high",
      "patterns": [
        {
          "source": "User data is limited to 16384 bytes",
          "flags": "i"
        }
      ],
      "explanation": "EC2 refused the instance or launch-template request because the rendered user data exceeds the 16,384-byte limit. The limit applies to the raw, base64-decoded user data, and CloudFormation resolves Fn::Sub and Fn::Base64 before EC2 sees the result, so a template that looks small can exceed the limit after substitution.",
      "verification": [
        "Measure what was actually sent, not the template text: base64-decode the rendered user data and count its bytes.",
        "For an existing instance, read the current value without changing anything: `aws ec2 describe-instance-attribute --instance-id <id> --attribute userData --query 'UserData.Value' --output text | base64 --decode | wc -c`.",
        "Shrink the inline script first - strip comments and blank lines - or move the bootstrap body to S3 or an SSM document, keep a short download-and-run stub in user data, and grant the instance profile read access to it.",
        "cloud-init also accepts gzip-compressed user data, and setup that rarely changes belongs in the AMI image rather than in the boot script."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/user-data.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "ec2.network-interface.create-failed",
      "title": "EC2 could not create a network interface",
      "confidence": "low",
      "patterns": [
        {
          "source": "(?:Error:\\s*creating EC2 Network Interface|Failed to (?:CreateNetworkInterface|create network interface)|failed to create (?:an )?network interface|error creating (?:an )?network interface).{0,320}\\boperation error EC2:\\s*CreateNetworkInterface\\b",
          "flags": "i"
        }
      ],
      "explanation": "The outer provider or deployment wrapper only identifies an EC2 CreateNetworkInterface call that failed. The nested HTTP status and error code are the useful evidence: common causes include subnet address exhaustion, security-group limits, a missing ec2:CreateNetworkInterface permission, an invalid IP or tag request, or an endpoint or emulator that does not implement the operation.",
      "verification": [
        "Preserve the complete nested line, including the HTTP status, request ID, error code, Region, subnet, and security groups; do not diagnose the wrapper without that cause.",
        "If the nested error names a subnet or address limit, run `aws ec2 describe-subnets --subnet-ids <subnet-id> --region <region>` and check available addresses and subnet sizing before retrying.",
        "If it names UnauthorizedOperation or AccessDenied, confirm the deployment identity has the least-privilege `ec2:CreateNetworkInterface` grant on the intended subnet and security groups; if it names InvalidParameterValue, correct the request shape or quota named by the message.",
        "When a custom endpoint or emulator handled the call, verify that it supports the EC2 CreateNetworkInterface operation before changing the infrastructure configuration."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_CreateNetworkInterface.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "eks.network-policy.agent-failed",
      "title": "The EKS VPC CNI network policy agent could not set up policy",
      "confidence": "medium",
      "patterns": [
        {
          "source": "failed to setup (?:default )?network policy\\b",
          "flags": "i"
        },
        {
          "source": "Network policy agent returned\\b",
          "flags": "i"
        }
      ],
      "explanation": "The Amazon VPC CNI network-policy agent failed while setting up a pod's policy at sandbox creation. This is an EKS add-on or node-level network-policy problem; the application workload and IAM policy are not the first things to change.",
      "verification": [
        "Record the pod, node, namespace, and timestamp, then inspect the `aws-network-policy-agent` container in the `aws-node` DaemonSet for the nested eBPF, veth, or PolicyEndpoint error.",
        "Confirm the VPC CNI add-on version and network-policy configuration meet the current EKS prerequisites, and check that the node kernel and architecture support the enabled policy mode.",
        "If the failure is transient and a kubelet retry succeeds, preserve both events and check the network-policy-agent and VPC CNI versions before changing policies; do not broaden application IAM for this node-level error."
      ],
      "documentation_url": "https://docs.aws.amazon.com/eks/latest/userguide/network-policies-troubleshooting.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "eks.vpc-cni.pod-sandbox-network-failed",
      "title": "The EKS VPC CNI could not set up pod networking",
      "confidence": "low",
      "patterns": [
        {
          "source": "Failed to create pod sandbox\\b.{0,500}\\baws-cni\\b.{0,220}\\bfailed\\b",
          "flags": "i"
        },
        {
          "source": "plugin type=[\\\"']?aws-cni[\\\"']?\\b.{0,220}\\bfailed\\b",
          "flags": "i"
        }
      ],
      "explanation": "The Kubernetes runtime could not create a pod sandbox through the Amazon VPC CNI. This wrapper does not contain the root cause; the aws-node or ipamd log usually names subnet or prefix exhaustion, ENI or instance limits, CNI permissions, subnet selection, or an unhealthy add-on.",
      "verification": [
        "Record the pod, node, Availability Zone, and timestamp, then inspect the matching aws-node DaemonSet or ipamd log for the nested EC2 status and error code; do not stop at the kubelet wrapper.",
        "If the nested message names unavailable IPs or prefixes, check the node's subnet and the instance ENI/IP limits with `aws ec2 describe-subnets --subnet-ids <subnet-id> --region <region>` and review the VPC CNI prefix or secondary-IP configuration.",
        "If it names UnauthorizedOperation or AccessDenied, verify the node or VPC CNI IAM role includes the least-privilege EC2 network-interface actions; if the add-on is unhealthy, check the aws-node DaemonSet status and version before changing workloads.",
        "Retry only after the nested CNI or EC2 cause is corrected; when a complete `CreateNetworkInterface` response is present, use that more specific finding instead of this wrapper handoff."
      ],
      "documentation_url": "https://docs.aws.amazon.com/eks/latest/userguide/managing-vpc-cni.html",
      "suppressed_by": [
        {
          "source": "\\boperation error EC2:\\s*CreateNetworkInterface\\b",
          "flags": "i"
        },
        {
          "source": "failed to setup (?:default )?network policy\\b",
          "flags": "i"
        },
        {
          "source": "Network policy agent returned\\b",
          "flags": "i"
        }
      ],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "kubernetes.pod-sandbox.network-setup-failed",
      "title": "Kubernetes could not set up a pod sandbox network",
      "confidence": "low",
      "patterns": [
        {
          "source": "(?:FailedCreatePodSandBox|Failed to create pod sandbox)\\b.{0,500}\\bfailed to set ?up network for sandbox\\b",
          "flags": "i"
        },
        {
          "source": "failed to set up sandbox container\\b.{0,500}\\bnetworkPlugin cni failed to set up Pod\\b",
          "flags": "i"
        }
      ],
      "explanation": "The kubelet could not create a pod sandbox because the CNI network setup stage failed. This wrapper does not identify the plugin's root cause, so inspect the plugin and node evidence before changing the workload or granting AWS permissions.",
      "verification": [
        "Capture the pod, namespace, node, and timestamp with the complete `FailedCreatePodSandBox` event, including the `plugin type` and nested error when present.",
        "Identify the CNI plugin from the event, then inspect its node DaemonSet or host log at the same timestamp; for EKS, check `aws-node` and `ipamd` before changing the workload.",
        "If the nested message names an AWS API denial, subnet or ENI limit, or network-policy agent failure, use that specific finding and verify the named cause; do not broaden IAM for the sandbox wrapper alone."
      ],
      "documentation_url": "https://kubernetes.io/docs/concepts/extend-kubernetes/compute-storage-net/network-plugins/",
      "suppressed_by": [
        {
          "source": "Failed to create pod sandbox\\b.{0,500}\\baws-cni\\b.{0,220}\\bfailed\\b",
          "flags": "i"
        },
        {
          "source": "plugin type=[\\\"']?aws-cni[\\\"']?\\b.{0,220}\\bfailed\\b",
          "flags": "i"
        },
        {
          "source": "failed to setup (?:default )?network policy\\b",
          "flags": "i"
        },
        {
          "source": "Network policy agent returned\\b",
          "flags": "i"
        }
      ],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "glue.database.rename-rejected",
      "title": "AWS Glue cannot rename an existing catalog database",
      "confidence": "high",
      "patterns": [
        {
          "source": "Database cannot be renamed\\b",
          "flags": "i"
        }
      ],
      "explanation": "AWS Glue rejected an UpdateDatabase request because the database name is immutable. The DatabaseInput.Name must remain the existing catalog name; description, location, parameters, and other mutable fields can be updated without renaming the database.",
      "verification": [
        "Read the current database definition with `aws glue get-database --name <database-name>` and preserve its exact catalog name in the UpdateDatabase request.",
        "Remove the rename from `DatabaseInput.Name` while updating the other intended fields, then retry the same Glue Region and CatalogId.",
        "If a new name is required, create a new database and migrate or recreate its tables and consumers before removing the old database; do not change IAM permissions for this validation error."
      ],
      "documentation_url": "https://docs.aws.amazon.com/glue/latest/webapi/API_UpdateDatabase.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudcontrol.operation.incomplete",
      "title": "The Cloud Control API operation did not complete",
      "confidence": "low",
      "patterns": [
        {
          "source": "AWS SDK Go Service Operation Incomplete\\b",
          "flags": "i"
        }
      ],
      "explanation": "The AWS Cloud Control API or its SDK wrapper stopped waiting for a resource operation, but this label is not the underlying failure. The nested StatusMessage, ErrorCode, resource type, identifier, and request token identify whether the handler, schema, service role, resource state, or a downstream AWS service failed.",
      "verification": [
        "Preserve the complete nested Cloud Control response, including OperationStatus, ErrorCode, StatusMessage, TypeName, Identifier, Region, and RequestToken.",
        "When a request token is present, run `aws cloudcontrol get-resource-request-status --request-token <token>` in the same account and Region to retrieve the latest ProgressEvent.",
        "Follow the nested status message to the resource handler, schema, service role, or downstream service before retrying; do not change IAM or retry blindly based only on this SDK wrapper."
      ],
      "documentation_url": "https://docs.aws.amazon.com/cloudcontrolapi/latest/APIReference/API_GetResourceRequestStatus.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "ecs.execute-command.agent-unavailable",
      "title": "ECS Exec could not start the managed agent",
      "confidence": "medium",
      "patterns": [
        {
          "source": "CannotStartManagedAgentError\\b",
          "flags": "i"
        },
        {
          "source": "InvalidParameterException\\b.{0,120}\\bwhen calling (?:the )?ExecuteCommand operation\\b",
          "flags": "i"
        },
        {
          "source": "execute command failed because execute command was not enabled when the task was run or the execute command agent (?:isn't|is not) running",
          "flags": "i"
        }
      ],
      "explanation": "ECS could not start the managed SSM agent needed by `execute-command`. The wrapper does not distinguish a task launched without Exec enabled from an agent, task-role, network, or filesystem prerequisite that is missing, so verify the task's reported state before changing IAM.",
      "verification": [
        "Read the task's Exec state with `aws ecs describe-tasks --cluster <cluster> --tasks <task>` and check `enableExecuteCommand` plus the `ExecuteCommandAgent` managed-agent `lastStatus` and reason.",
        "Ensure the task was launched after Exec was enabled, its task role allows the required `ssmmessages` channel actions, and the task can reach the Systems Manager Message Gateway endpoints (or the corresponding VPC endpoint).",
        "ECS Exec requires a writable container filesystem; remove `readonlyRootFilesystem` or mounts that hide the managed-agent paths, then launch a new task before retrying."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-exec.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.lambda-layer.artifact-unreadable",
      "title": "CloudFormation cannot read a Lambda layer artifact from S3",
      "confidence": "high",
      "patterns": [
        {
          "source": "access has been denied by S3.*permission.*GetObject",
          "flags": "i"
        },
        {
          "source": "permission to GetObject for.*bucket",
          "flags": "i"
        }
      ],
      "explanation": "The deployment could not retrieve the S3 object used for a Lambda layer. The deployment or CloudFormation execution identity needs access to the exact artifact, and encryption controls can impose an additional requirement.",
      "verification": [
        "Identify the identity that CloudFormation uses for the stack operation and the exact layer archive object ARN.",
        "Allow `s3:GetObject` for that object while checking bucket-policy denies and cross-account ownership.",
        "If the artifact uses a customer-managed KMS key, verify the identity also has the required decrypt permission."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-properties-lambda-layerversion-content.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "sam.build.output-permission-denied",
      "title": "SAM build cannot access its generated build directory",
      "confidence": "medium",
      "patterns": [
        {
          "source": "(?:PermissionError|Permission denied|Access is denied).{0,180}\\.aws-sam[\\\\/]+build\\b",
          "flags": "i"
        },
        {
          "source": "\\.aws-sam[\\\\/]+build\\b.{0,180}(?:PermissionError|Permission denied|Access is denied)",
          "flags": "i"
        }
      ],
      "explanation": "SAM CLI could not read, write, or replace a generated file under `.aws-sam/build`. This is a local filesystem ownership, permission, or file-lock problem before deployment, not an AWS resource or IAM failure.",
      "verification": [
        "Close editors, file watchers, antivirus scans, and other processes that may hold the generated file, then rerun `sam build --debug` and preserve the first complete permission error.",
        "Inspect the owner and permissions of `.aws-sam/build` and the named file; on Windows use `icacls .aws-sam\\build`, and on Unix use `ls -ld .aws-sam/build` plus the file path.",
        "After confirming the source template and function directories are safe, move or remove only the generated `.aws-sam/build` directory and rebuild; do not broaden AWS permissions or delete source artifacts to fix a local lock."
      ],
      "documentation_url": "https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/sam-cli-command-reference-sam-build.html",
      "suppressed_by": [],
      "excluded_line_patterns": [
        {
          "source": "unable to unlink old",
          "flags": "i"
        }
      ],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "sam.build.docker-required",
      "title": "SAM build requires Docker for containerized builds",
      "confidence": "high",
      "patterns": [
        {
          "source": "Cannot connect to the Docker daemon",
          "flags": "i"
        },
        {
          "source": "Error:\\s*Docker is unavailable or not running",
          "flags": "i"
        },
        {
          "source": "Building image for .* requires Docker\\.",
          "flags": "i"
        },
        {
          "source": "sam build --use-container.*(?:cannot execute|executable file not found|not found|is not recognized|command not found)",
          "flags": "i"
        },
        {
          "source": "is the docker daemon running\\?",
          "flags": "i"
        },
        {
          "source": "is docker running\\?",
          "flags": "i"
        },
        {
          "source": "No such file or directory.*docker\\.sock",
          "flags": "i"
        },
        {
          "source": "sh: docker: not found",
          "flags": "i"
        }
      ],
      "explanation": "The build job reached a containerized SAM build path and could not reach a usable Docker daemon. Container builds require Docker even for transient validation steps in CI.",
      "verification": [
        "Run `docker version` on the failing runner and confirm Docker is started.",
        "If using a self-hosted runner, check permissions for `docker.sock` and that the Docker service is running.",
        "If Docker is not available in your environment, disable containerized build paths (`sam build` without `--use-container`) and retry."
      ],
      "documentation_url": "https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/sam-cli-command-reference-sam-build.html#sam-cli-command-reference-sam-build-use-container",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "docker.registry.image-unavailable",
      "title": "The build could not pull a container image from the registry",
      "confidence": "high",
      "patterns": [
        {
          "source": "pull access denied for \\S+",
          "flags": "i"
        },
        {
          "source": "repository does not exist or may require ['\\\"]?docker login",
          "flags": "i"
        },
        {
          "source": "manifest for \\S+ not found",
          "flags": "i"
        },
        {
          "source": "manifest unknown",
          "flags": "i"
        }
      ],
      "explanation": "Docker reached the registry and was refused, or asked for a tag the registry does not have. The daemon is running - it returned this error - so this is not a Docker-availability problem. Registries answer an unauthenticated request for a private image and a request for a missing image almost identically, which is why the message mentions both possibilities: either the runner is not logged in to that registry, or the image and tag really are absent (a tag that was never pushed, or one built for a different architecture).",
      "verification": [
        "Pull the exact image and tag from the failing runner to reproduce it outside SAM: `docker pull <image>:<tag>`.",
        "For a private registry, log in first in the same job - `aws ecr get-login-password | docker login --username AWS --password-stdin <registry>` for ECR, or the equivalent action - and confirm the login step runs before the build.",
        "Confirm the tag exists: `aws ecr describe-images --repository-name <repo> --image-ids imageTag=<tag>`, or the registry's own listing.",
        "Check the image's architecture matches the function: an arm64-only image requested for an x86_64 build reports as a missing manifest."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AmazonECR/latest/userguide/registry_auth.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "build.host.disk-full",
      "title": "The build host ran out of disk space",
      "confidence": "high",
      "patterns": [
        {
          "source": "no space left on device",
          "flags": "i"
        },
        {
          "source": "\\bENOSPC\\b",
          "flags": "i"
        }
      ],
      "explanation": "The build wrote until the filesystem was full. On a hosted CI runner this is usually not the project's own output: a container build keeps every intermediate layer, dependency caches accumulate across steps, and the runner image itself already occupies much of the disk. The failure often surfaces in an unrelated-looking step - whichever one happened to need the next block - so the step that reports it is not necessarily the step at fault.",
      "verification": [
        "Print the disk state at the point of failure to confirm it and see what is consuming it: `df -h` and `docker system df`.",
        "Reclaim space in the job before the build step: `docker system prune -af` and remove any large caches restored earlier in the workflow.",
        "On GitHub-hosted runners, free the preinstalled toolchains you do not use (the several gigabytes under `/usr/share/dotnet`, `/usr/local/lib/android`, `/opt/ghc`) before building.",
        "If the build genuinely needs more room than the runner has, move it to a larger runner rather than pruning further."
      ],
      "documentation_url": "https://docs.docker.com/engine/manage-resources/pruning/",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "lambda.package.size-limit-exceeded",
      "title": "The Lambda deployment package exceeds a per-function size limit",
      "confidence": "high",
      "patterns": [
        {
          "source": "Unzipped size must be smaller than",
          "flags": "i"
        },
        {
          "source": "Request must be smaller than .* bytes",
          "flags": "i"
        }
      ],
      "explanation": "The function's code package - whether measured zipped or after unzipping - exceeds one of AWS Lambda's per-function size limits, so CreateFunction/UpdateFunctionCode failed. This is a per-function packaging limit, not the regional code-storage quota.",
      "verification": [
        "Measure the built artifact size locally before deploying to catch this before AWS does.",
        "List the largest files inside the zip to find what's bloating the package (e.g. `unzip -l function.zip | sort -k1 -n -r | head`).",
        "Move shared dependencies into a Lambda layer, trim dev/test-only dependencies from the deployment package, or switch to a container image (up to 10 GB) if the package is genuinely large."
      ],
      "documentation_url": "https://docs.aws.amazon.com/lambda/latest/dg/gettingstarted-limits.html",
      "suppressed_by": [],
      "excluded_line_patterns": [
        {
          "source": "CodeStorageExceededException",
          "flags": "i"
        },
        {
          "source": "Code storage limit exceeded",
          "flags": "i"
        }
      ],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "lambda.layers.size-limit-exceeded",
      "title": "Attached layers exceed the Lambda function's available size",
      "confidence": "high",
      "patterns": [
        {
          "source": "Layers consume more than the available size of",
          "flags": "i"
        }
      ],
      "explanation": "Lambda adds up the unzipped bytes of the function package and every attached layer version, and rejects CreateFunction, UpdateFunctionCode, and UpdateFunctionConfiguration when the total passes 262,144,000 bytes (250 MB). The check is on the combined total, so attaching one more layer - or a new version of a layer that grew - can fail a function whose own package never changed.",
      "verification": [
        "Sum what Lambda actually measures: the unzipped bytes of the function package plus every attached layer version, not the zipped upload sizes.",
        "Read the attached layers without changing anything: `aws lambda get-function --function-name <name> --query '{code:Configuration.CodeSize,layers:Configuration.Layers}'` lists each layer ARN with its zipped CodeSize; download and unzip the heaviest to count its real bytes.",
        "Drop the layers the function no longer imports, and trim the remaining ones - tests, docs, `__pycache__`, and unstripped native libraries are usually where the bytes went.",
        "If the dependencies are genuinely that large, package the function as a container image (up to 10 GB) or load bulky assets from EFS or S3 at runtime instead of shipping them in layers."
      ],
      "documentation_url": "https://docs.aws.amazon.com/lambda/latest/dg/gettingstarted-limits.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "apigateway.security-policy.endpoint-access-required",
      "title": "API Gateway needs an endpoint access mode for its security policy",
      "confidence": "high",
      "patterns": [
        {
          "source": "Endpoint access mode is required for the specified security policy\\b",
          "flags": "i"
        }
      ],
      "explanation": "API Gateway rejected an enhanced `SecurityPolicy_*` because the REST API or custom domain did not declare an endpoint access mode. This is a resource-property mismatch, not an IAM failure.",
      "verification": [
        "Read the API or custom-domain resource and confirm whether its `SecurityPolicy` starts with `SecurityPolicy_`; enhanced policies require an explicit endpoint access mode.",
        "Set `EndpointAccessMode` to the mode that matches the intended traffic (`BASIC` or `STRICT`) on `AWS::Serverless::Api`, `AWS::ApiGateway::RestApi`, or the relevant domain resource, then verify the transformed template.",
        "If the API is defined through OpenAPI, keep the equivalent `x-amazon-apigateway-endpoint-access-mode` extension in the generated definition and retry; do not change IAM for this validation error."
      ],
      "documentation_url": "https://docs.aws.amazon.com/apigateway/latest/developerguide/apigateway-security-policies.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "apigateway.deployment.no-methods",
      "title": "API Gateway deployment started before the API had any methods",
      "confidence": "high",
      "patterns": [
        {
          "source": "The REST API does(?:n't| not) contain any methods",
          "flags": "i"
        }
      ],
      "explanation": "API Gateway rejected the deployment because no methods existed when the deployment resource was created. This often happens when a SAM-generated deployment is combined with a manually declared `AWS::ApiGateway::Deployment`.",
      "verification": [
        "Check whether the template declares a manual `AWS::ApiGateway::Deployment` alongside `AWS::Serverless::Api`.",
        "Prefer SAM's generated API deployment, or add `DependsOn` entries for every required API method when managing deployments manually.",
        "Review the transformed template and the affected API Gateway methods before redeploying."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-apigateway-deployment.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.resource.stabilization-timeout",
      "title": "A resource was accepted by its service but never reached a stable state",
      "confidence": "high",
      "patterns": [
        {
          "source": "did not stabilize",
          "flags": "i"
        },
        {
          "source": "HandlerErrorCode:\\s*NotStabilized",
          "flags": "i"
        },
        {
          "source": "Exceeded attempts to wait",
          "flags": "i"
        }
      ],
      "explanation": "The service accepted CloudFormation's create or update call, but the resource never reached its steady state within the wait window, so CloudFormation gave up and failed the operation. The stabilization wording is only the wrapper: the real cause is the nested status reason from the service handler when the log includes one, a resource that is genuinely slow to provision, or a handler that never signals completion.",
      "verification": [
        "Find the quoted message inside `Resource handler returned message` (or the resource's own event in the service console) and treat that nested reason as the failure to fix; only fall back to the guidance below when there is none.",
        "For slow-by-design resources - ACM certificates waiting on DNS validation, CloudFront distributions, RDS instances - confirm the external dependency (validation records, quotas) and retry; the same template often succeeds once the dependency clears.",
        "For custom resources and third-party handlers, check the handler function's own logs for the invocation: a handler that crashed or never sent its response leaves CloudFormation waiting until the timeout."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/troubleshooting.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": true
    },
    {
      "id": "cloudformation.nested-stack.propagation-failed",
      "title": "A nested (embedded) stack failed - the root cause is in the child stack's events",
      "confidence": "high",
      "patterns": [
        {
          "source": "Embedded stack .* was not successfully (?:created|updated)",
          "flags": "i"
        },
        {
          "source": "Embedded stack .*The following resource\\(s\\) failed to (?:create|update):",
          "flags": "i"
        }
      ],
      "explanation": "The parent stack's message only says an embedded stack failed - that is a symptom, not the cause. The real root cause lives in the child stack's own event stream, and CloudFormation can delete the child stack (and its events) as part of rollback before anyone reads them.",
      "verification": [
        "Capture the child stack's events before rollback deletes them, or redeploy with `--disable-rollback` to keep the stack around while debugging.",
        "Run `aws cloudformation describe-stack-events --stack-name <child-stack-arn>` with the full nested stack ARN from this message - it still works once deletion has started.",
        "Find the child stack's first `CREATE_FAILED` or `UPDATE_FAILED` resource event and fix that; only edit the parent template after that."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/using-cfn-nested-stacks.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "lambda.runtime.deprecated",
      "title": "AWS Lambda runtime is no longer supported for deployment",
      "confidence": "high",
      "patterns": [
        {
          "source": "(?:\\b(?:CREATE_FAILED|UPDATE_FAILED)\\b.{0,1000}|\\bInvalidParameterValueException\\b.{0,500})The runtime parameter of [A-Za-z0-9._-]+ is no longer supported for creating or updating AWS Lambda functions",
          "flags": "i"
        }
      ],
      "explanation": "Lambda rejected the function create or update because the template declares a runtime that has reached the stage of deprecation where AWS no longer accepts deployments for it. Retrying the same template cannot succeed; the function must move to a currently supported runtime.",
      "verification": [
        "Inspect the function's `Runtime` in the submitted template and in `.aws-sam/build/template.yaml`, if SAM generated that deployment artifact.",
        "Compare that value with the supported runtimes and deprecation dates in the AWS Lambda runtime documentation.",
        "Before changing `Runtime:`, review language and dependency compatibility and confirm the matching local interpreter or build image is available; then rebuild and test before redeploying."
      ],
      "documentation_url": "https://docs.aws.amazon.com/lambda/latest/dg/lambda-runtimes.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.resource.create-update-failed",
      "title": "CloudFormation resource creation or update failed",
      "confidence": "high",
      "patterns": [
        {
          "source": "\\bCREATE_FAILED\\b",
          "flags": "i"
        },
        {
          "source": "\\bUPDATE_FAILED\\b",
          "flags": "i"
        }
      ],
      "explanation": "A CloudFormation resource failed before the stack rollback completed. Its status reason is usually the most direct evidence for the root cause.",
      "verification": [
        "Identify the failed logical resource ID and preserve its exact status reason.",
        "Check the underlying service event or API error named in that status reason.",
        "Fix the resource-level cause before retrying the stack operation."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/view-stack-events.html",
      "suppressed_by": [
        {
          "source": "(?:access has been denied by S3[\\s\\S]*permission[\\s\\S]*GetObject)",
          "flags": "i"
        },
        {
          "source": "Code signing is not supported for functions created with container images",
          "flags": "i"
        },
        {
          "source": "Lambda does not have permission to access the ECR image",
          "flags": "i"
        }
      ],
      "excluded_line_patterns": [
        {
          "source": "Has prohibited field Resource",
          "flags": "i"
        },
        {
          "source": "Error Code:\\s*InvalidBucketName",
          "flags": "i"
        },
        {
          "source": "The specified bucket is not valid",
          "flags": "i"
        },
        {
          "source": "BucketAlreadyExists",
          "flags": "i"
        },
        {
          "source": "BucketAlreadyOwnedByYou",
          "flags": "i"
        },
        {
          "source": "The REST API does(?:n't| not) contain any methods",
          "flags": "i"
        },
        {
          "source": "Character sets beyond ASCII are not supported",
          "flags": "i"
        },
        {
          "source": "did not stabilize",
          "flags": "i"
        },
        {
          "source": "HandlerErrorCode:\\s*NotStabilized",
          "flags": "i"
        },
        {
          "source": "Exceeded attempts to wait",
          "flags": "i"
        },
        {
          "source": "cannot be (?:updated|deleted) as it is in use by",
          "flags": "i"
        },
        {
          "source": "CodeStorageExceededException",
          "flags": "i"
        },
        {
          "source": "Code storage limit exceeded",
          "flags": "i"
        },
        {
          "source": "Maximum policy size of 10240 bytes exceeded for (?:the )?role\\b",
          "flags": "i"
        },
        {
          "source": "Cannot exceed quota for PoliciesPerRole: \\d+",
          "flags": "i"
        },
        {
          "source": "ReservedConcurrentExecutions for function decreases account's UnreservedConcurrentExecution below its minimum value",
          "flags": "i"
        },
        {
          "source": "Embedded stack .* was not successfully (?:created|updated)",
          "flags": "i"
        },
        {
          "source": "User is not authorized to access connection\\b.*Service:\\s*AWSCodeBuild\\b.*Error Code:\\s*OAuthProviderException\\b",
          "flags": "i"
        },
        {
          "source": "AbortIncompleteMultipartUpload cannot be specified with Tags",
          "flags": "i"
        },
        {
          "source": "(?:following )?resource\\s+['\\\"]?ImageRecipe['\\\"]?\\s+already exists",
          "flags": "i"
        },
        {
          "source": "ResourceAlreadyExistsException.*(?:ImageRecipe|CreateImageRecipe)",
          "flags": "i"
        },
        {
          "source": "(?:ImageRecipe|CreateImageRecipe).*ResourceAlreadyExistsException",
          "flags": "i"
        },
        {
          "source": "An error occurred \\((?:ServiceNotAvailable|ServiceUnavailable)\\) when calling the (?:CreateStack|UpdateStack|CreateChangeSet|ExecuteChangeSet) operation",
          "flags": "i"
        },
        {
          "source": "(?:ServiceUnavailableException|ServiceNotAvailable).{0,160}(?:CloudFormation|CreateStack|UpdateStack|CreateChangeSet|ExecuteChangeSet)",
          "flags": "i"
        },
        {
          "source": "Failed to create/update (?:the )?stack\\b",
          "flags": "i"
        },
        {
          "source": "Template format error:\\s*Unresolved resource dependencies\\b",
          "flags": "i"
        },
        {
          "source": "Lambda was unable to configure (?:access to )?your environment variables.{0,800}KMS Exception:\\s*(?:AccessDenied|Disabled|KMSInvalidState|InvalidArn|NotFound)Exception\\b",
          "flags": "i"
        },
        {
          "source": "KMS Exception:\\s*AccessDeniedException\\b.{0,240}ciphertext refers to a customer master key that does not exist,\\s*does not exist in this region,\\s*or you are not allowed to access\\.?",
          "flags": "i"
        },
        {
          "source": "Endpoint access mode is required for the specified security policy\\b",
          "flags": "i"
        },
        {
          "source": "An error occurred \\(TooManyRequestsException\\) when calling the (?:CreateApi|CreateDeployment|CreateRestApi|CreateStage|CreateUsagePlan|ImportApi|ImportRestApi|PutRestApi|ReimportApi|UpdateUsagePlan) operation\\b",
          "flags": "i"
        },
        {
          "source": "\\b(?:CREATE_FAILED|UPDATE_FAILED)\\b.{0,1000}Too Many Requests\\s*\\(Service:\\s*ApiGateway,\\s*Status Code:\\s*429\\b",
          "flags": "i"
        },
        {
          "source": "The runtime parameter of [A-Za-z0-9._-]+ is no longer supported for creating or updating AWS Lambda functions",
          "flags": "i"
        },
        {
          "source": "The provided execution role does not have permission(?:s)? to call CreateNetworkInterface on EC2\\b",
          "flags": "i"
        },
        {
          "source": "User data is limited to 16384 bytes",
          "flags": "i"
        },
        {
          "source": "Layers consume more than the available size of",
          "flags": "i"
        },
        {
          "source": "\\bCircular dependency between resources\\b(?::|$)",
          "flags": "i"
        },
        {
          "source": "\\bNo export named\\s+\\S+\\s+(?:can be found|found)\\b",
          "flags": "i"
        }
      ],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.resource.property-non-ascii",
      "title": "A resource property was rejected for non-ASCII characters",
      "confidence": "high",
      "patterns": [
        {
          "source": "Character sets beyond ASCII are not supported",
          "flags": "i"
        }
      ],
      "explanation": "A resource handler rejected a property value because it contains a character outside ASCII - commonly an em dash, a smart quote, or an arrow pasted in from a design document or word processor. The rejected parameter or property is named earlier in the evidence line; the handler does not identify every offending code point or rule out other constraints on the same value.",
      "verification": [
        "Identify the rejected parameter or property named in the evidence line.",
        "Inspect the exact submitted or generated template, and any parameter or `Fn::Sub` source that produced the value; check `.aws-sam/build/template.yaml` too if a SAM build artifact was deployed.",
        "Find non-ASCII characters locally: `python -c \"from pathlib import Path; p=Path('template.yaml'); print([n for n,s in enumerate(p.read_text(encoding='utf-8').splitlines(),1) if not s.isascii()])\"`.",
        "Replace the character with wording allowed by the property's documented character set, rather than deleting the value."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-ec2-securitygroup.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "lambda.env-vars.kms-key-inaccessible",
      "title": "Lambda could not use the KMS key for its environment variables",
      "confidence": "high",
      "patterns": [
        {
          "source": "Lambda was unable to configure (?:access to )?your environment variables.{0,800}KMS Exception:\\s*(?:AccessDenied|Disabled|KMSInvalidState|InvalidArn|NotFound)Exception\\b",
          "flags": "i"
        },
        {
          "source": "KMS Exception:\\s*AccessDeniedException\\b.{0,240}ciphertext refers to a customer master key that does not exist,\\s*does not exist in this region,\\s*or you are not allowed to access\\.?",
          "flags": "i"
        }
      ],
      "explanation": "Lambda encrypts environment variables with a customer-managed KMS key, and it could not use the configured key. The wrapper error is a Lambda `InvalidParameterValueException`, which reads like a bad template value - the real cause is the `KMS Exception:` inside it. Three causes account for nearly all of these, and they need different fixes: the key policy or grants do not let the deploying principal, Lambda service, or function execution role use the key (`AccessDeniedException`); the key is disabled or pending deletion, which fails even when every policy is correct (`DisabledException`, `KMSInvalidStateException`); or the key ARN is malformed, or names another Region or account (`InvalidArnException`, `NotFoundException`), which no permission change can fix.",
      "verification": [
        "Read the exception name after `KMS Exception:` first - it separates a policy problem from a key-state problem from a bad ARN.",
        "Confirm the key exists and check its state in the function's own Region: `aws kms describe-key --key-id <key-arn>`, then read `KeyState` (a `Disabled` or `PendingDeletion` key fails this way regardless of policy).",
        "Review the key policy and grants for both the deploying principal and the function's execution role; do not begin by broadly changing IAM identity policies.",
        "Confirm `kms:CreateGrant`, `kms:Encrypt`, and `kms:DescribeKey` for the deployment and Lambda grant path, plus `kms:Decrypt` wherever the execution role actually decrypts values.",
        "If the ARN is malformed or points at another Region, correct `KmsKeyArn` in the template - a Lambda function can only use a key in its own Region. For a cross-account key, confirm the owning account's key policy and any existing grant permit the deployment path.",
        "After correcting the key policy, key state, or ARN, re-save or re-encrypt the function's environment configuration and redeploy it."
      ],
      "documentation_url": "https://docs.aws.amazon.com/lambda/latest/dg/configuration-envvars.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "lambda.code-storage.limit-exceeded",
      "title": "AWS Lambda code storage limit exceeded",
      "confidence": "high",
      "patterns": [
        {
          "source": "CodeStorageExceededException",
          "flags": "i"
        },
        {
          "source": "Code storage limit exceeded",
          "flags": "i"
        }
      ],
      "explanation": "AWS Lambda reached the regional code storage limit (default 75 GB) for your account. This total includes all present and past deployed function code, layers, and saved function versions across the region.",
      "verification": [
        "Check total or current storage usage using `aws lambda get-account-settings`.",
        "List unused or old function versions using `aws lambda list-versions-by-function`.",
        "Delete unnecessary versions with `aws lambda delete-function --qualifier <version>` or set up automated lifecycle cleanup."
      ],
      "documentation_url": "https://docs.aws.amazon.com/lambda/latest/dg/gettingstarted-limits.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "lambda.concurrency.reserved-below-minimum",
      "title": "Reserved concurrency would drop the account below its minimum unreserved value",
      "confidence": "high",
      "patterns": [
        {
          "source": "ReservedConcurrentExecutions for function decreases account's UnreservedConcurrentExecution below its minimum value",
          "flags": "i"
        }
      ],
      "explanation": "The account's total concurrency limit minus every function's `ReservedConcurrentExecutions` must leave at least the account minimum (usually 100) unreserved for functions that reserve nothing. This is an account-level ceiling, not a template-syntax or permissions problem - it is especially common on a fresh account, where the concurrency limit can still be at the 1,000 default, or lower in a new or burst-limited account, sometimes as low as 10-50, where no reservation is possible at all.",
      "verification": [
        "Run `aws lambda get-account-settings` (read-only) to see the account's concurrency limit and its current unreserved value.",
        "Sum `ReservedConcurrentExecutions` across every function in the account, not just the one in this template, to see how much is already committed.",
        "Either lower or remove the reservation in the template, or request a concurrency quota increase for the account and retry."
      ],
      "documentation_url": "https://docs.aws.amazon.com/lambda/latest/dg/lambda-concurrency.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.stack.create-name-conflict",
      "title": "A CloudFormation create operation targeted an existing stack name",
      "confidence": "high",
      "patterns": [
        {
          "source": "An error occurred \\(AlreadyExistsException\\) when calling the CreateStack operation:\\s*Stack\\s+\\[[^\\]\\r\\n]+\\]\\s+already exists\\b",
          "flags": "i"
        },
        {
          "source": "An error occurred \\(ValidationError\\) when calling the CreateChangeSet operation:\\s*Stack\\s+\\[[^\\]\\r\\n]+\\]\\s+already exists and cannot be created again with the changeSet\\s+\\[[^\\]\\r\\n]+\\]",
          "flags": "i"
        }
      ],
      "explanation": "CloudFormation received a create operation for a stack name that already exists in the selected account and Region. This can be an intended stack that should use an update-capable path, a wrong stack name/profile/Region or SAM configuration environment, or a different deployment that owns the name. The error alone is not evidence that deletion is safe.",
      "verification": [
        "Confirm the caller and target Region, then inspect the exact stack read-only with aws sts get-caller-identity and aws cloudformation describe-stacks --stack-name <stack> --region <region> --query 'Stacks[0].[StackId,StackStatus,CreationTime]' --output table.",
        "If the stack is REVIEW_IN_PROGRESS, inspect its change sets before changing anything with aws cloudformation list-change-sets --stack-name <stack> --region <region>.",
        "Use an update-capable path only for the intended, updatable stack; otherwise correct --stack-name, profile, Region, samconfig.toml environment, or change-set type, or choose another name for a different deployment.",
        "Do not treat a failed describe-stacks permission check as proof that the stack is absent; branch only on CloudFormation's specific not-found response.",
        "Do not delete a stack from this error alone. Review its identity, status, retained resources, and termination protection before choosing a cleanup path."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/APIReference/API_CreateChangeSet.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.stack.operation-in-progress",
      "title": "Another CloudFormation operation is already in progress on the stack",
      "confidence": "high",
      "patterns": [
        {
          "source": "is in (?:CREATE|UPDATE|DELETE)_IN_PROGRESS state and (?:can not|cannot) be updated",
          "flags": "i"
        },
        {
          "source": "OperationInProgressException",
          "flags": "i"
        }
      ],
      "explanation": "CloudFormation rejected the deployment because another operation is still running on the same stack - a concurrent CI run, a teammate's deploy, or a console operation. The new operation cannot start until the in-flight one finishes.",
      "verification": [
        "Check for a concurrent deployment against the same stack and let it finish before retrying.",
        "Run `aws cloudformation describe-stack-events --stack-name <stack>` (read-only) to see the in-flight operation and how far it has progressed.",
        "Serialize CI deploys to the same stack with a GitHub Actions `concurrency` group keyed by stack name."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/troubleshooting.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.stack.failed-recreate-required",
      "title": "A terminal CloudFormation stack must be recreated before deployment",
      "confidence": "high",
      "patterns": [
        {
          "source": "(?:ROLLBACK_COMPLETE|DELETE_COMPLETE).*(?:can not|cannot) be updated",
          "flags": "i"
        },
        {
          "source": "is in (?:ROLLBACK_COMPLETE|DELETE_COMPLETE) state.*(?:can not|cannot) be updated",
          "flags": "i"
        }
      ],
      "explanation": "CloudFormation cannot update a stack in the terminal `ROLLBACK_COMPLETE` or `DELETE_COMPLETE` state. A rollback-complete stack needs its original create failure fixed before recreation; a delete-complete stack needs a new create operation after deletion and any retained resources are checked.",
      "verification": [
        "Confirm the terminal state and inspect the stack events with `aws cloudformation describe-stack-events --stack-name <stack>` (read-only).",
        "For `ROLLBACK_COMPLETE`, find the first earlier `CREATE_FAILED` resource event and fix that underlying cause before recreating the stack.",
        "For `DELETE_COMPLETE`, wait for deletion to finish, check any retained resources and custom names, then create the stack again instead of sending an update.",
        "Do not delete or retain resources blindly; confirm the stack state and intended cleanup path in CloudFormation first."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/view-stack-events.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.stack.update-rollback-failed",
      "title": "Stack rollback itself failed and must be continued or skipped",
      "confidence": "high",
      "patterns": [
        {
          "source": "UPDATE_ROLLBACK_FAILED.*(?:can not|cannot) be updated",
          "flags": "i"
        },
        {
          "source": "is in UPDATE_ROLLBACK_FAILED state.*(?:can not|cannot) be updated",
          "flags": "i"
        }
      ],
      "explanation": "CloudFormation tried to roll back a failed update, but the rollback itself failed and the stack is stuck in UPDATE_ROLLBACK_FAILED. No new operation can start until the rollback is continued past, or explicitly skips, the resource that would not roll back.",
      "verification": [
        "Find the resource event that caused the rollback to fail in `aws cloudformation describe-stack-events`.",
        "Run `aws cloudformation continue-update-rollback --stack-name <stack>`, adding `--resources-to-skip <resource>` only for resources confirmed unable to roll back.",
        "Confirm the stack reaches UPDATE_ROLLBACK_COMPLETE before retrying the deploy."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/troubleshooting.html#troubleshooting-errors-update-rollback-failed",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.stack.rollback-complete",
      "title": "CloudFormation stack entered rollback after an earlier resource failure",
      "confidence": "medium",
      "patterns": [
        {
          "source": "ROLLBACK_IN_PROGRESS",
          "flags": "i"
        },
        {
          "source": "ROLLBACK_FAILED",
          "flags": "i"
        },
        {
          "source": "ROLLBACK_COMPLETE",
          "flags": "i"
        },
        {
          "source": "UPDATE_ROLLBACK",
          "flags": "i"
        }
      ],
      "explanation": "Rollback is a downstream stack state. The most useful evidence is usually the first failed resource event that appears before the rollback entries.",
      "verification": [
        "Inspect stack events in chronological order and locate the first `CREATE_FAILED` or `UPDATE_FAILED` resource.",
        "If the stack is in `ROLLBACK_FAILED`, inspect the failed cleanup event and resolve that blocker before retrying the rollback or deletion.",
        "Preserve the exact resource status reason before retrying the deployment.",
        "Use a change set or isolated stack when testing a fix, where practical."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/determine-root-cause-for-stack-failures.html",
      "suppressed_by": [
        {
          "source": "ROLLBACK_COMPLETE.*(?:can not|cannot) be updated",
          "flags": "i"
        },
        {
          "source": "UPDATE_ROLLBACK_FAILED.*(?:can not|cannot) be updated",
          "flags": "i"
        },
        {
          "source": "following resource\\(s\\) failed to delete",
          "flags": "i"
        },
        {
          "source": "failed to delete.*AWS::IAM::Role",
          "flags": "i"
        },
        {
          "source": "Unable to delete.*AWS::IAM::Role",
          "flags": "i"
        },
        {
          "source": "Embedded stack .* was not successfully (?:created|updated)",
          "flags": "i"
        },
        {
          "source": "\\bCircular dependency between resources\\b(?::|$)",
          "flags": "i"
        },
        {
          "source": "\\bNo export named\\s+\\S+\\s+(?:can be found|found)\\b",
          "flags": "i"
        }
      ],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.rollback.iam-role-delete-failed",
      "title": "CloudFormation rollback could not delete an IAM role",
      "confidence": "medium",
      "patterns": [
        {
          "source": "The following resource\\(s\\) failed to delete:.*Role",
          "flags": "i"
        },
        {
          "source": "failed to delete.*IAM Role",
          "flags": "i"
        },
        {
          "source": "Unable to delete.*AWS::IAM::Role",
          "flags": "i"
        }
      ],
      "explanation": "CloudFormation could not remove an IAM role during rollback or deletion. The rollback may stop even after the root failure is fixed, blocking clean retries until the dependency is released.",
      "verification": [
        "Find the IAM role and inspect its attached managed policies, inline policies, and role attachments (including instance profiles).",
        "Temporarily detach blockers or confirm deletion permissions, then retry rollback or delete the stack with resources retained as required.",
        "Re-run deployment only after the stack can transition cleanly past the rollback phase."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/using-cfn-deleting-stack.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.stack.termination-protection",
      "title": "Stack deletion is blocked by termination protection",
      "confidence": "high",
      "patterns": [
        {
          "source": "cannot be deleted while TerminationProtection is enabled",
          "flags": "i"
        }
      ],
      "explanation": "CloudFormation refused the delete because termination protection is enabled on the stack. Someone turned that protection on deliberately, so the refusal is a safeguard rather than a failure.",
      "verification": [
        "Confirm the stack name, account, and Region match the stack you intend to remove.",
        "Find out why termination protection was enabled before removing it; the stack may be shared or load-bearing.",
        "Only after confirming the stack is safe to remove, disable protection (`aws cloudformation update-termination-protection --no-enable-termination-protection`) and retry the delete."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/using-cfn-protect-stacks.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.stack.delete-failed",
      "title": "CloudFormation could not delete one or more stack resources",
      "confidence": "medium",
      "patterns": [
        {
          "source": "\\bDELETE_FAILED\\b",
          "flags": "i"
        }
      ],
      "explanation": "A stack or resource deletion failed. The status reason usually names the blocker directly: a non-empty S3 bucket, a network interface still attached to a Lambda function, a nested stack with its own failure, or a resource another stack depends on.",
      "verification": [
        "Identify the blocking resource and preserve its exact status reason before changing anything.",
        "Resolve the blocker deliberately - empty the bucket, wait for or detach the network interface, fix the nested stack - rather than force-deleting anything.",
        "If a resource should survive the stack, retry with `aws cloudformation delete-stack --retain-resources` for that logical ID after the blocker is understood."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/troubleshooting.html",
      "suppressed_by": [
        {
          "source": "The following resource\\(s\\) failed to delete:.*Role\\b",
          "flags": "i"
        },
        {
          "source": "failed to delete.*IAM Role",
          "flags": "i"
        },
        {
          "source": "Unable to delete.*AWS::IAM::Role",
          "flags": "i"
        },
        {
          "source": "cannot be (?:updated|deleted) as it is in use by",
          "flags": "i"
        }
      ],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.export.in-use",
      "title": "A stack export cannot change while another stack imports it",
      "confidence": "high",
      "patterns": [
        {
          "source": "Export\\s+\\S+\\s+cannot be (?:updated|deleted) as it is in use by",
          "flags": "i"
        },
        {
          "source": "Cannot (?:update|delete) (?:an )?export\\s+\\S+\\s+as it is in use",
          "flags": "i"
        }
      ],
      "explanation": "CloudFormation refused to change or remove an exported output because at least one other stack imports it with `Fn::ImportValue`. This is a dependency safeguard, not a failure in the exporting stack: consumers pin the export's name and value until they stop importing it.",
      "verification": [
        "List every consumer with `aws cloudformation list-imports --export-name <name>` and record which stacks pin the export.",
        "Migrate in stages: add a new export alongside the old one, update each consumer stack to import the new name, then remove the old export once `list-imports` shows no consumers.",
        "Do not delete or force-update the consumer stacks as a shortcut - they are load-bearing for whoever owns them; coordinate the migration with those owners instead."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/using-cfn-stack-exports.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.export.not-found",
      "title": "CloudFormation could not find the imported stack export",
      "confidence": "high",
      "patterns": [
        {
          "source": "\\bNo export named\\s+\\S+\\s+(?:can be found|found)\\b",
          "flags": "i"
        }
      ],
      "explanation": "The consumer stack references an export name that CloudFormation cannot resolve in the target account and Region. The producer stack may not have been deployed yet, its output name may have changed, or the deployment is pointed at a different account or Region. This is a cross-stack dependency problem, not an IAM denial or a transient rollback state.",
      "verification": [
        "Preserve the exact export name from the error and confirm the consumer's account and Region before changing either stack.",
        "List exports in that same account and Region with `aws cloudformation list-exports --region <region>` and check whether the name exists.",
        "Inspect the producer stack's `Outputs` and `Export: Name` value, then deploy or update the producer before the consumer; do not rename or remove an export that other stacks still import.",
        "If the stacks are intentionally cross-account or cross-Region, replace the `Fn::ImportValue` design with an explicitly supported handoff such as a parameter or `Fn::GetStackOutput` rather than expecting a local export to resolve."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/using-cfn-stack-exports.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.capabilities.required",
      "title": "CloudFormation needs an explicit capability acknowledgement",
      "confidence": "high",
      "patterns": [
        {
          "source": "InsufficientCapabilities(?:Exception)?",
          "flags": "i"
        },
        {
          "source": "Requires capabilities\\s*:\\s*\\[?CAPABILITY_(?:IAM|NAMED_IAM|AUTO_EXPAND)",
          "flags": "i"
        }
      ],
      "explanation": "CloudFormation rejected the change set because the deployment did not explicitly acknowledge a capability required by the template. The error identifies the capability that must be reviewed before retrying.",
      "verification": [
        "Read the capability named in the error and inspect the relevant template resources before changing deployment settings.",
        "For IAM resources, configure `CAPABILITY_IAM`; use `CAPABILITY_NAMED_IAM` when the template gives IAM resources custom names.",
        "For nested applications, configure `CAPABILITY_AUTO_EXPAND`, then review the expanded application and proposed change set."
      ],
      "documentation_url": "https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/sam-cli-command-reference-sam-deploy.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.template.quota-exceeded",
      "title": "The template exceeds a CloudFormation size or count quota",
      "confidence": "medium",
      "patterns": [
        {
          "source": "Value at 'template(?:Body|URL)' failed to satisfy constraint: Member must have length less than or equal to",
          "flags": "i"
        },
        {
          "source": "Template format error: Number of \\S+ .{0,40}is greater than maximum allowed",
          "flags": "i"
        },
        {
          "source": "Template (?:body )?may not exceed [\\d,]+ bytes",
          "flags": "i"
        }
      ],
      "explanation": "CloudFormation rejected the template itself, before evaluating any resource, because it is past a service quota: the template body is larger than the limit for how it was submitted (51,200 bytes inline, 460,800 bytes from S3), or it declares more resources, parameters, or outputs than a stack may hold. No template change other than shrinking or splitting it will get past this.",
      "verification": [
        "Measure the rendered template before deploying - `wc -c .aws-sam/build/template.yaml` - and compare it with the byte limit named in the error.",
        "Submit the template through S3 rather than inline: `sam deploy --resolve-s3` (or an explicit `--s3-bucket`) raises the ceiling to 460,800 bytes, and `aws cloudformation validate-template --template-url <s3-url>` confirms it read-only.",
        "For a count quota, read the number the error reports and move part of the stack into nested stacks or a separate stack; the CloudFormation quotas page lists the current per-stack maximums."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/cloudformation-limits.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "sam.deploy.bucket-config-conflict",
      "title": "SAM deployment configured both a managed and explicit S3 bucket",
      "confidence": "high",
      "patterns": [
        {
          "source": "Cannot use both --resolve-s3 and --s3-bucket parameters",
          "flags": "i"
        }
      ],
      "explanation": "The deployment selected two mutually exclusive artifact-bucket mechanisms. SAM cannot both resolve a managed S3 bucket and use an explicit `--s3-bucket` in the same command.",
      "verification": [
        "Inspect the selected `samconfig.toml` environment and the workflow's `sam deploy` arguments together.",
        "For a pipeline that passes `--s3-bucket`, disable `resolve_s3` in that deployment configuration.",
        "Alternatively, remove the explicit bucket and let SAM resolve its managed bucket; keep exactly one mechanism."
      ],
      "documentation_url": "https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/sam-cli-command-reference-sam-deploy.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "sam.build.esbuild-missing",
      "title": "SAM build cannot find the configured esbuild dependency",
      "confidence": "high",
      "patterns": [
        {
          "source": "NodejsNpmEsbuildBuilder:EsbuildBundle.*(?:Cannot|can not) find esbuild",
          "flags": "i"
        },
        {
          "source": "Esbuild Failed:\\s*(?:Cannot|can not) find esbuild",
          "flags": "i"
        }
      ],
      "explanation": "A function configured with `BuildMethod: esbuild` reached SAM's esbuild builder, but the bundler was not available in the project or runner environment used for the build.",
      "verification": [
        "Declare a compatible `esbuild` version in the function project's development dependencies and commit its lockfile.",
        "Run the matching package-manager install step before `sam build` in CI.",
        "Confirm the workflow builds the same directory that contains the function's `package.json`."
      ],
      "documentation_url": "https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/building-typescript.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "sam.build.python-dependency-resolution-failed",
      "title": "SAM Python dependency resolution failed",
      "confidence": "high",
      "patterns": [
        {
          "source": "PythonPipBuilder:ResolveDependencies\\s+-",
          "flags": "i"
        },
        {
          "source": "Could not find a version that satisfies the requirement",
          "flags": "i"
        }
      ],
      "explanation": "SAM could not resolve one or more Python dependencies during build. The failure usually comes from a pinned package/version that is not available for the build environment.",
      "verification": [
        "Check the package and pin in the function dependency file and confirm it is available for the target architecture and Python version.",
        "Reproduce with a local `pip install` using the same build-time Python version to confirm the missing/invalid requirement.",
        "Align constraints with a build-compatible version or wheel source and retry."
      ],
      "documentation_url": "https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/sam-cli-command-reference-sam-build.html",
      "suppressed_by": [
        {
          "source": "Binary validation failed",
          "flags": "i"
        }
      ],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "sam.build.python-runtime-mismatch",
      "title": "SAM Python runtime binary is incompatible with the build runtime",
      "confidence": "high",
      "patterns": [
        {
          "source": "PythonPipBuilder:Validation - Binary validation failed for python",
          "flags": "i"
        },
        {
          "source": "Python executable.*not found",
          "flags": "i"
        },
        {
          "source": "Your build environment.*runtime.*python",
          "flags": "i"
        },
        {
          "source": "The runtime python[0-9.]+ binary was not found",
          "flags": "i"
        },
        {
          "source": "Do you have python for runtime",
          "flags": "i"
        }
      ],
      "explanation": "SAM reached Python binary validation and confirmed the builder did not have a matching interpreter for the SAM function runtime.",
      "verification": [
        "Compare the function runtime in the template (for example `python3.12`) to the local/interpreter runtime used by the build.",
        "Use a matching SAM build image or pinned runtime image that provides the required Python binary.",
        "If using native extensions, prefer `sam build --use-container` with a container that matches the SAM runtime."
      ],
      "documentation_url": "https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/sam-cli-command-reference-sam-build.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "sam.build.python-dependency-validation-failed",
      "title": "SAM Python dependency build validation failed",
      "confidence": "high",
      "patterns": [
        {
          "source": "Binary validation failed",
          "flags": "i"
        }
      ],
      "explanation": "SAM reached the Python dependency validation path and failed before a build artifact was produced. Keep the fix specific to the highest-confidence scenario in the failure output.",
      "verification": [
        "Compare the Python runtime in the build environment with the runtime declared in the SAM template.",
        "Reproduce locally with `sam build --debug` to capture the full dependency failure and missing module output.",
        "Use lockfile-pinned dependencies and compatible wheels, then retry with a clean build environment."
      ],
      "documentation_url": "https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/sam-cli-command-reference-sam-build.html",
      "suppressed_by": [
        {
          "source": "PythonPipBuilder:Validation - Binary validation failed",
          "flags": "i"
        },
        {
          "source": "Python executable.*not found",
          "flags": "i"
        },
        {
          "source": "Could not find a version that satisfies the requirement",
          "flags": "i"
        }
      ],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "cloudformation.deploy.no-changes",
      "title": "The deployment failed only because there were no changes to deploy",
      "confidence": "high",
      "patterns": [
        {
          "source": "No changes to deploy\\.?\\s*Stack .{0,120}is up to date",
          "flags": "i"
        },
        {
          "source": "The submitted information didn't contain changes",
          "flags": "i"
        },
        {
          "source": "No updates are to be performed",
          "flags": "i"
        }
      ],
      "explanation": "The stack already matches the template, so CloudFormation produced an empty change set and the deploy command exited with an error. In a pipeline this is usually a configuration choice, not a real failure.",
      "verification": [
        "For automated pipelines, pass `--no-fail-on-empty-changeset` to `sam deploy` (or set `fail_on_empty_changeset = false` in `samconfig.toml`); `aws cloudformation deploy` supports the same flag.",
        "If this run was expected to change the stack, confirm the build step ran and produced updated artifacts before the deploy step.",
        "Confirm the deploy targeted the intended stack name, region, and configuration environment."
      ],
      "documentation_url": "https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/sam-cli-command-reference-sam-deploy.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "sam.deploy.artifact-upload-failed",
      "title": "SAM could not upload a build artifact referenced by the template",
      "confidence": "high",
      "patterns": [
        {
          "source": "Unable to upload artifact .{0,120}referenced by (?:CodeUri|ContentUri|DefinitionUri)",
          "flags": "i"
        },
        {
          "source": "Parameter (?:CodeUri|ContentUri|DefinitionUri) of resource .{0,120}refers to a file or folder that does not exist",
          "flags": "i"
        }
      ],
      "explanation": "A `CodeUri`, `ContentUri`, or `DefinitionUri` path in the template does not exist where the deploy ran. Usually `sam build` was never run, the deploy ran from a different directory than the build, or the source template was deployed instead of `.aws-sam/build/template.yaml`.",
      "verification": [
        "Run `sam build` first and deploy the built template it produces.",
        "Confirm the CI job checks out the repository and runs build and deploy in the same working directory.",
        "Verify the referenced path exists in the environment that runs the deploy, not only on the machine that authored the template."
      ],
      "documentation_url": "https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/sam-cli-command-reference-sam-build.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "sam.deploy.configuration-resolution-failed",
      "title": "AWS SAM deployment configuration or parameter resolution failed",
      "confidence": "medium",
      "patterns": [
        {
          "source": "Unable to locate credentials",
          "flags": "i"
        },
        {
          "source": "Parameter.*must have values",
          "flags": "i"
        },
        {
          "source": "Error: Failed to create changeset",
          "flags": "i"
        },
        {
          "source": "Failed to create (?:the )?changeset\\b",
          "flags": "i"
        },
        {
          "source": "sam deploy.{0,80}(?:failed|error|unable)",
          "flags": "i"
        }
      ],
      "explanation": "The deployment failed before or while CloudFormation created a change set. Confirm the selected SAM configuration environment, AWS identity, required parameters, and CloudFormation capabilities.",
      "verification": [
        "Run `sam validate --lint` locally and confirm the selected `samconfig.toml` environment.",
        "Verify that required parameter overrides and secrets are available to the deployment job.",
        "Review the complete change-set error before changing the template."
      ],
      "documentation_url": "https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/using-sam-cli-deploy.html",
      "suppressed_by": [
        {
          "source": "InsufficientCapabilities",
          "flags": "i"
        },
        {
          "source": "Requires capabilities",
          "flags": "i"
        },
        {
          "source": "Cannot use both --resolve-s3 and --s3-bucket",
          "flags": "i"
        },
        {
          "source": "Esbuild Failed:\\s*(?:Cannot|can not) find esbuild",
          "flags": "i"
        },
        {
          "source": "PythonPipBuilder:ResolveDependencies\\s+-",
          "flags": "i"
        },
        {
          "source": "property\\s+\\S+:\\s+not defined for resource of type AWS::Serverless::",
          "flags": "i"
        },
        {
          "source": "Error Code:\\s*InvalidBucketName",
          "flags": "i"
        },
        {
          "source": "The specified bucket is not valid",
          "flags": "i"
        },
        {
          "source": "BucketAlreadyExists",
          "flags": "i"
        },
        {
          "source": "BucketAlreadyOwnedByYou",
          "flags": "i"
        },
        {
          "source": "ExpiredToken(?:Exception)?",
          "flags": "i"
        },
        {
          "source": "security token included in the request is expired",
          "flags": "i"
        },
        {
          "source": "Signature expired:.*is now earlier than",
          "flags": "i"
        },
        {
          "source": "An error occurred \\(Throttling\\)",
          "flags": "i"
        },
        {
          "source": "\\bRate exceeded\\b",
          "flags": "i"
        },
        {
          "source": "An error occurred \\(TooManyRequestsException\\) when calling the (?:CreateApi|CreateDeployment|CreateRestApi|CreateStage|CreateUsagePlan|ImportApi|ImportRestApi|PutRestApi|ReimportApi|UpdateUsagePlan) operation\\b",
          "flags": "i"
        },
        {
          "source": "\\b(?:CREATE_FAILED|UPDATE_FAILED)\\b.{0,1000}Too Many Requests\\s*\\(Service:\\s*ApiGateway,\\s*Status Code:\\s*429\\b",
          "flags": "i"
        },
        {
          "source": "No changes to deploy",
          "flags": "i"
        },
        {
          "source": "The submitted information didn't contain changes",
          "flags": "i"
        },
        {
          "source": "No updates are to be performed",
          "flags": "i"
        },
        {
          "source": "An error occurred \\(AlreadyExistsException\\) when calling the CreateStack operation:\\s*Stack\\s+\\[[^\\]\\r\\n]+\\]\\s+already exists\\b",
          "flags": "i"
        },
        {
          "source": "An error occurred \\(ValidationError\\) when calling the CreateChangeSet operation:\\s*Stack\\s+\\[[^\\]\\r\\n]+\\]\\s+already exists and cannot be created again with the changeSet\\s+\\[[^\\]\\r\\n]+\\]",
          "flags": "i"
        },
        {
          "source": "is in (?:CREATE|UPDATE|DELETE)_IN_PROGRESS state and (?:can not|cannot) be updated",
          "flags": "i"
        },
        {
          "source": "is in ROLLBACK_COMPLETE state and (?:can not|cannot) be updated",
          "flags": "i"
        },
        {
          "source": "OperationInProgressException",
          "flags": "i"
        },
        {
          "source": "Unable to upload artifact",
          "flags": "i"
        },
        {
          "source": "refers to a file or folder that does not exist",
          "flags": "i"
        },
        {
          "source": "Value at 'template(?:Body|URL)' failed to satisfy constraint: Member must have length less than or equal to",
          "flags": "i"
        },
        {
          "source": "Template format error: Number of \\S+ .{0,40}is greater than maximum allowed",
          "flags": "i"
        },
        {
          "source": "Template (?:body )?may not exceed [\\d,]+ bytes",
          "flags": "i"
        },
        {
          "source": "S3 error: Access ?Denied",
          "flags": "i"
        },
        {
          "source": "\\bCircular dependency between resources\\b(?::|$)",
          "flags": "i"
        },
        {
          "source": "\\bNo export named\\s+\\S+\\s+(?:can be found|found)\\b",
          "flags": "i"
        },
        {
          "source": "Parameters:[ \\t]*\\[ssm(?:-secure)?:[^\\r\\n\\]]{1,200}\\][ \\t]*cannot be found\\b",
          "flags": "i"
        },
        {
          "source": "SSM parameter[ \\t]+\\S{1,200}[ \\t]+not found\\b",
          "flags": "i"
        }
      ],
      "excluded_line_patterns": [
        {
          "source": "every Fn::GetAtt object requires two non-empty parameters",
          "flags": "i"
        },
        {
          "source": "Template format error:\\s*Unresolved resource dependencies\\b",
          "flags": "i"
        }
      ],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "ssm.parameter.resolution-failed",
      "title": "An SSM parameter referenced by the template could not be resolved",
      "confidence": "high",
      "patterns": [
        {
          "source": "Parameters:[ \\t]*\\[ssm(?:-secure)?:[^\\r\\n\\]]{1,200}\\][ \\t]*cannot be found\\b",
          "flags": "i"
        },
        {
          "source": "SSM parameter[ \\t]+\\S{1,200}[ \\t]+not found\\b",
          "flags": "i"
        }
      ],
      "explanation": "A `{{resolve:ssm:...}}` or `{{resolve:ssm-secure:...}}` dynamic reference, or an SSM parameter type, could not be resolved. These resolve at change-set time, in the target account and Region, using the deployment's own credentials - so a parameter that exists locally, or in the account you develop in, proves nothing about the account being deployed to. The usual causes are a path that encodes an environment name that does not match the target (`/my-app/prod/...` deployed to staging), a new environment whose parameters were never seeded, a deployment pointed at a different Region than the parameter, or - for `ssm-secure` - a principal that can read the parameter but cannot decrypt it.",
      "verification": [
        "Look up the exact path from the error using the same credentials and Region the deployment used: `aws ssm get-parameter --name <name> --region <region>`.",
        "Compare the path against the target environment's naming: an environment segment left at another stage's value is the most common cause.",
        "If the environment is new, seed the parameter in the target account and Region before re-deploying - the template cannot create what it resolves.",
        "For `ssm-secure`, add `--with-decryption` to that lookup, and confirm the deploy principal has `ssm:GetParameters` plus `kms:Decrypt` on the key protecting the parameter.",
        "Confirm the deployment's Region matches where the parameter lives; a correct path in the wrong Region reports as missing."
      ],
      "documentation_url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/dynamic-references.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "sam.deploy.interactive-confirmation-required",
      "title": "SAM deployment prompted for interactive changeset confirmation",
      "confidence": "medium",
      "patterns": [
        {
          "source": "Deploy this changeset\\?\\s*\\[y/N\\]:",
          "flags": "i"
        }
      ],
      "explanation": "SAM stopped at an interactive confirm step and could not continue in a non-interactive pipeline.",
      "verification": [
        "Set `--no-confirm-changeset` (or `confirm_changeset: false`) for automated pipelines.",
        "Move manual approval to a GitHub environment or protected workflow gate instead of interactive prompt output.",
        "Re-run the deployment after removing the interactive confirmation path."
      ],
      "documentation_url": "https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/serverless-sam-cli-using-git.html",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    },
    {
      "id": "apigateway.cors.preflight-conflict",
      "title": "API Gateway CORS preflight configuration conflicts with an existing OPTIONS method",
      "confidence": "medium",
      "patterns": [
        {
          "source": "duplicate.*OPTIONS",
          "flags": "i"
        },
        {
          "source": "OPTIONS.*(?:already exists|duplicate)",
          "flags": "i"
        },
        {
          "source": "(?:CORS|preflight).{0,80}(?:conflict|duplicate|overlap)",
          "flags": "i"
        }
      ],
      "explanation": "SAM can generate CORS preflight handling. Defining an overlapping OPTIONS method or mixing manual and generated CORS configuration can create a conflict.",
      "verification": [
        "Check whether the API definition already declares an `OPTIONS` method for the affected path.",
        "Use either SAM-managed CORS or a fully manual preflight implementation for that route, not both.",
        "Verify the generated API definition before redeploying."
      ],
      "documentation_url": "https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/sam-resource-api.html#sam-api-cors",
      "suppressed_by": [],
      "excluded_line_patterns": [],
      "parse_denial_context": false,
      "parse_stabilization_context": false
    }
  ],
  "samples": [
    {
      "name": "oidc-assume-role-failure.txt",
      "label": "GitHub Actions OIDC role assumption",
      "log": "Run aws-actions/configure-aws-credentials@v4\nError: Not authorized to perform: sts:AssumeRoleWithWebIdentity\nAn error occurred (AccessDenied) when calling the AssumeRoleWithWebIdentity operation: Not authorized to perform sts:AssumeRoleWithWebIdentity\nRole ARN: arn:aws:iam::123456789012:role/github-production-deploy\nWorkflow actor: builder@example.com\n"
    },
    {
      "name": "cloudformation-resource-failure.txt",
      "label": "CloudFormation resource failure",
      "log": "2026-08-02 14:22:10 UTC  MyApiDeployment  CREATE_FAILED  Resource handler returned message: \"Invalid request provided: API Gateway deployment cannot be created because the stage already exists.\" (RequestToken: example, HandlerErrorCode: InvalidRequest)\n2026-08-02 14:22:12 UTC  DemoStack        ROLLBACK_IN_PROGRESS  The following resource(s) failed to create: [MyApiDeployment]. Rollback requested by user.\n"
    },
    {
      "name": "capability-acknowledgement-failure.txt",
      "label": "Missing IAM capability",
      "log": "Deploying with following values\n===============================\nStack name                   : example-service\nCapabilities                 : []\n\nError: Failed to create changeset for the stack: example-service\nAn error occurred (InsufficientCapabilitiesException) when calling the CreateChangeSet operation: Requires capabilities : [CAPABILITY_NAMED_IAM]\n"
    },
    {
      "name": "python-pip-build-failure.txt",
      "label": "Python build dependency failure",
      "log": "PythonPipBuilder:ResolveDependencies - Binary validation failed: failed to build package cryptography\nThe runtime python3.10 binary was not found for this build environment.\nUse a compatible build image with matching architecture and Python version.\n"
    },
    {
      "name": "s3-bucket-conflict-failure.txt",
      "label": "S3 bucket configuration conflict",
      "log": "Error: Cannot use both --resolve-s3 and --s3-bucket parameters. Please use only one.\n"
    },
    {
      "name": "interactive-changeset-failure.txt",
      "label": "Interactive changeset prompt",
      "log": "Deploy this changeset? [y/N]:\nAborted!\n"
    },
    {
      "name": "esbuild-missing-failure.txt",
      "label": "esbuild not found",
      "log": "NodejsNpmEsbuildBuilder:EsbuildBundle - Esbuild Failed: Cannot find esbuild.\n"
    },
    {
      "name": "api-gateway-no-methods-failure.txt",
      "label": "API Gateway stage has no methods",
      "log": "2026-08-03 11:02:41 UTC  MyApiDeployment  CREATE_FAILED  Resource handler returned message: \"The REST API doesn't contain any methods.\"\n"
    }
  ]
};
